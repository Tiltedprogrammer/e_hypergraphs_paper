\chapter{String Diagram Rewriting}\label{chapter:string_diagrams}

E-graphs can be seen as a particular flavour of term graphs~\cite{plump_term,EggsAreAdhesive} that is equipped with a congruence relation.
In our journey towards a categorical interpretation of e-graphs and their generalisations, we may view string diagrams as a particular flavour of term graphs that are available for more general theories.
String diagrams being a graphical syntax for morphisms in monoidal categories are completely formalised objects on their own: they are given by nodes and curves embedded into a plane quotiented by the topological deformations (isotopy).
While this view of string diagrams is very handy when reasoning about monoidal categories using pen and paper, it is not very convenient for computer-aided reasoning.
Luckily, this gap of inconvenience was filled by a series of works initiated by Bonchi et. al.~\cite{bonchi_string_2022-1,bonchi_string_2022-2} consequently picked up and extended by other authors~\cite{zanassi_comonoid,ghica2024stringdiagramslambdacalculifunctional,ghica_rewriting_2023}.
In their works, string diagrams are formalised as concrete combinatorial objects, called hypergraphs, that are just collections of vertices and hyperedges.
This view turns out to absorb many structural equivalences between morphisms of monoidal categories: unlike in string diagrams where such equivalences are stipulated by either isotopy or by equations, in hypergraph they are built-in into the notion of isomorphism which is much simpler.
A great benefit of this view is that it allows for a definition of rewriting of such hypergraphs which automatically happens modulo much of the symmetric monoidal structure, making it practically appealing.
We will view our generalised e-graphs as string diagrams for a particular monoidal category and hence as a certain kind of hypergraphs, defining a suitable notion of rewriting for them.
We will then use this representation in showing that the ordinary e-graphs are recovered as a special case of these hypergraphs.
Before we can do that, we need to recall the necessary machinery introduced by Bonchi et. el. in their works.
This is the content of this chapter.

\section{String diagrams}

String diagrams are traditionally attributed to Penrose~\cite{penrose1984spinors}, who used diagrams for combining linear operators.
Now the (variations of) diagrams are prevalent in many areas of mathematics and computer science, both as an efficient device for calculating with parallel processes and as an aid to understanding complex phenomena such as quantum processes (via ZX-calculus) and higher category theory (\textit{cf.} \texttt{homotopy.io} proof assistant~\cite{homotopy_io}).
Below we give a brief account of string diagrams as a syntax for morphisms in a (symmetric) monoidal category as developed by Joyal and Street~\cite{joyal_geometry_1991}, who first made precise the folklore that string diagrams are sound and complete syntax for morphisms in a monoidal category.

The basic building blocks of their theory are \emph{generalised graphs} $\Gamma$, which are given by a topological space with extra structure.
There's a dedicated \emph{discrete} (finite) subset of \emph{nodes} $\Gamma_0$ and a finite number of subsets $\Gamma \setminus \Gamma_0$ called \emph{wires} each of which is a connected component in the appropriate sense locally behaving like an interval in $\mathbb{R}$\footnote{Formally this is stipulated by saying that this complement is a 1-manifold.}, which in particular means that one can go only either up or down the wire.
Each connected component also includes its endpoints which are furthermore ordered to prescribe the orientation to each wire.
A node $x$ is said to have a \emph{degree} $n$ if the number of connected components of $V \setminus \{x\}$ is $n$ where $V$ is a small neighbourhood of $x$.
Then, a certain subset of nodes $\partial \Gamma$ is identified as the \emph{boundary} of the graph, which is the set of nodes of degree one.
We call a graph \emph{progressive} if it has no directed cycles and will consider only such graphs in this overview.

In the plain monoidal case (without symmetry), such graphs are embedded into a strip of $\mathbb{R}^{2}$.
\begin{definition}[Chapter 1. Section 2~\cite{joyal_geometry_1991}]
Let $a < b$ be real numbers.
A \textit{progressive plane graph} is a graph $\Gamma$ with boundary embedded in $\mathbb{R} \times [a,b]$ such that 
\begin{itemize}
    \item $\partial \Gamma = \Gamma \cap (\mathbb{R} \times \{a,b\})$,
    \item the second projection $\pi_{2} : \mathbb{R} \times [a,b] \to [a,b]$ is injective on each connected component of $\Gamma \setminus \Gamma_0$.
\end{itemize}
\end{definition}
Intuitively, the strip has an unbounded width and a fixed height $[a,b]$.
Condition (i) says the graph meets the top and bottom edges of the strip exactly at its boundary nodes, and nowhere else; condition (ii) says no wire ever bends back downward. 
The embedding guarantees that the wires do not cross each other and the nodes are not on top of each other.
With this in place, it makes sense to draw an example.

\begin{example}
    Below, there is one example and three non-examples of progressive plane graphs.
    We depict the nodes of $\Gamma_0$ as $\node$ and depict the wires as curves connecting the nodes, and the nodes of $\partial \Gamma$ as $\bnode$.
    The strip of the plane is depicted by the dashed lines.

\begin{center}
\begin{tabular}{cccc}
\tikzfig{./figures/string/planar_graph_example} &
\tikzfig{./figures/string/planar_graph_non_example_1} &
\tikzfig{./figures/string/planar_graph_non_example_2} &
\tikzfig{./figures/string/planar_graph_non_example_3}\\
(i) & (ii) & (iii) & (iv)
\end{tabular}
\end{center}
The graph (i) is a valid progressive plane graph; the graph (ii) fails to be a progressive plane graph because the wires cross, so it fails to be an embedding---two wires intersect at a point which is not a node; the graph (iii) fails to be a progressive plane graph because a wire bends back and furthermore the first condition above is not satisfied as not all nodes of $\partial \Gamma$ lie on the boundary lines $\mathbb{R} \times \{a,b\}$; finally, the graph (iv) fails to be a progressive plane graph because a wire has three endpoints and thus does not behave locally as an interval in $\mathbb{R}$, in particular, where the wire splits there's an option to go in three directions.
\end{example}
Because each wire is monotone in height, the wires crossing any horizontal line $\mathbb{R} \times \{u\}$ inherit a left-to-right linear order from their first coordinate.
We write $\mathrm{dom}\,\Gamma$ and $\mathrm{cod}\,\Gamma$ for the boundary nodes on the bottom line $\mathbb{R} \times \{a\}$ and top line $\mathbb{R} \times \{b\}$ respectively, each linearly ordered left to right. 
For an inner node $x$, let $\mathrm{in}(x)$ and $\mathrm{out}(x)$ denote its incoming and outgoing wires, again linearly ordered by the same device.
Given a plane graph $\Gamma$, $\Gamma[c,d]$ is a subgraph of $\Gamma$ between the horizontal lines $\mathbb{R} \times \{c\}$ and $\mathbb{R} \times \{d\}$, also called a \emph{layer}, such that neither of the lines intersects any inner nodes, with the wires crossing the lines $c$ and $d$ becoming its domain and codomain respectively.
It is also possible to split a graph $\Gamma$ vertically if there exists a number $\xi$ such that
\[\Gamma_1 \subseteq (-\infty, \xi) \times [a,b] \quad \text{and} \quad \Gamma_2 \subseteq (\xi, \infty) \times [a,b]\]
so that the graph $\Gamma$ is a disjoint union of $\Gamma_1, \Gamma_2$, written $\Gamma_1 \otimes \Gamma_2$.

Given a strict monoidal category $\mathcal{C}$, we can assign to each graph $\Gamma$ a \emph{valuation} $v = (v_0, v_1)$ that assigns to each wire $w$ an object $v_0(w)$ of $\mathcal{C}$ and to each inner node $x$ a morphism $v_1(x)$ of $\mathcal{C}$ in a compatible way.
A graph together with a valuation is called a \emph{diagram}.
The \emph{value} of the diagram as a whole is then assigned by splitting the diagram horizontally and vertically into simple pieces as follows.
A graph $\Gamma$ is called \emph{primitive} if it is either a connected component with a single node, or a collection of connected components each of which is a single wire.
In the first case, let the single node be $x$; then the value $v(\Gamma) = v_1(x) : \mathop{\otimes}\limits_{i \in \mathrm{in}(x)}v_0(i) \to \mathop{\otimes}\limits_{j \in \mathrm{out}(x)}v_0(j)$ with $\mathrm{out}(x)$ and $\mathrm{in}(x)$ ordered.
In the second case let $\pi_0(\Gamma)$ be the set of connected components ordered by their intersection with a horizontal line; then the value $v(\Gamma) = \id_{\mathop{\otimes}\limits_{i \in \pi_0(\Gamma)}v_0(i)}$.
A graph $\Gamma$ is called \emph{elementary} if it is a tensor of primitive graphs, i.e., $\Gamma = \Gamma_1 \otimes \ldots \otimes \Gamma_n$ and then $v(\Gamma) = v(\Gamma_1) \otimes \ldots \otimes v(\Gamma_n)$.
A value of a general diagram $\Gamma$ is computed by slicing the diagram into elementary layers $\Gamma[a, c_1], \Gamma[c_1, c_2], \ldots, \Gamma[c_{n}, b]$ and taking $v(\Gamma) = v(\Gamma[c_n, b]) \circ v(\Gamma[c_{n-1}, c_{n}]) \circ \ldots \circ v(\Gamma[a, c_1])$.
A fundamental result of Joyal and Street is that the value of a diagram is invariant under deformation, which is defined next.

\begin{definition}[Chapter 1. Section 2~\cite{joyal_geometry_1991}]
Let $\Gamma, \partial\Gamma$ be a graph with boundary.
A deformation of progressive plane graphs (between levels $a$ and $b$) is a continuous function
\[
h : \Gamma \times [0,1] \to \mathbb{R} \times [a,b]
\]
such that for all $t \in [0,1]$, the function $h(-,t) : \Gamma \to \mathbb{R} \times [a,b]$ is an embedding whose image is a progressive plane graph $(\Gamma(t), \partial\Gamma(t))$ between the levels $a$ and $b$.
\end{definition}
Put simply, each $h(-,t)$ is a way to embed $\Gamma$ into the plane and the deformation is a continuous way to change this embedding.
\begin{example}
    \[
    \tikzfig{./figures/string/string_diagram_example_1}
    \]
    Above, the valuation of the inner nodes is given by labels $f,g,h,k$ which are appropriately typed morphisms; we assume that all objects of the category are given by tensors of a single object.
    When it is important, the wires can also be labelled by the types.
    One particular slicing of the diagram is shown with dashed boxes, each box denotes a \emph{primitive} diagram which is either a single inner node or a collection of wires with no inner nodes.
    A (horizontal) layer is a tensor product of primitive diagrams and the valuation of the whole diagram is then obtained by composing the valuations of the layers sequentially. 
    That is, the value of the diagram is 
    \[
   (\id \otimes h) \circ (f \otimes \id) \circ (g \otimes k) .
    \]
    We can bend the wires and slide the boxes around to obtain different embeddings of the same graph without changing the value.
\end{example}

Finally, it can be shown that diagrams embedded in a square strip $(-1,1) \times [-1,1]$, called \emph{boxed} diagrams, can be equipped with the structure of a monoidal category. 
The tensor of two such diagrams is obtained by translating the two graphs apart so that they occupy the left and right halves, taking their union, and rescaling the result back into the square. 
The unit is the empty diagram. 
Composition is given by stacking the two diagrams, joining the codomain wires of the lower diagram to the domain wires of the upper one by new wire segments, and again rescaling to fit the strip.
This data possesses a universal property which is what makes it useful for pen-and-paper reasoning about monoidal categories as described next.

\begin{definition}[Monoidal signature]\label{def:mon_signature}
A monoidal signature $\Sigma$ is a pair of sets $(\Sigma_0, \Sigma_1)$ together with two functions $\mathrm{dom}$ and $\mathrm{cod}$ from $\Sigma_1 \to \Sigma_0^{*}$ that assign to each \emph{generator} $f \in \Sigma_1$ a list of objects (a word) in $\Sigma_0$ as its domain and codomain respectively.
When $\Sigma_0 \cong \{*\}$ (and $\Sigma_0^{\ast} \cong \mathbb{N}$), the generators can be written as $f : n \to m$ where $n = \mathrm{dom}(f)$ and $m = \mathrm{cod}(f)$ simply mean the number of inputs and outputs of $f$, respectively.
\end{definition}
\begin{definition}\label{def:free}
Given a monoidal category $\mathcal{C}$, an \emph{interpretation} of a monoidal
signature $\Sigma$ in $\mathcal{C}$ assigns to each object of $\Sigma_0$ an object
of $\mathcal{C}$ and to each generator $d : X_1\cdots X_m \to Y_1 \cdots Y_n$ in
$\Sigma_1$ a morphism
$Kd : KX_1 \otimes \cdots \otimes KX_m \to KY_1 \otimes \cdots \otimes KY_n$ of
$\mathcal{C}$. Interpretations form a category $[\Sigma, \mathcal{C}]$: a morphism
$\kappa : K \to L$ is a family of morphisms $\kappa_X : KX \to LX$, one for each
$X \in \Sigma_0$, such that for every generator $d$ as above the square
% https://q.uiver.app/#q=
\[
\adjustbox{scale=0.8}{
\begin{tikzcd}
	{KX_1 \otimes \cdots \otimes KX_m} && {KY_1 \otimes\ldots\otimes KYn} \\
	\\
	{LX_1 \otimes\ldots \otimes LX_m} && {LY_1\otimes\ldots\otimes LY_n}
	\arrow["Kd", from=1-1, to=1-3]
	\arrow["{\kappa X_1 \otimes \ldots \kappa X_m}"', from=1-1, to=3-1]
	\arrow["{\kappa Y_1 \otimes \ldots \kappa Y_n}", from=1-3, to=3-3]
	\arrow["Yd"', from=3-1, to=3-3]
\end{tikzcd}}
\]
commutes.

A strict monoidal category $\mathcal{F}$, equipped with a distinguished
interpretation $N \in [\Sigma, \mathcal{F}]$, is called \emph{free} on $\Sigma$ if
for every monoidal category $\mathcal{C}$ and every interpretation
$K \in [\Sigma, \mathcal{C}]$ there is a unique strict monoidal functor
$F : \mathcal{F} \to \mathcal{C}$ with $F \circ N = K$.
\end{definition}
\begin{theorem}\label{theorem:free_string_planar}
For each monoidal signature $\Sigma$, the following data define a strict monoidal
category $\mathbb{F}(\Sigma)$.
\paragraph{Objects.} The objects are words in $\Sigma_{0}$.
\paragraph{Morphisms.} The morphisms are deformation classes of boxed progressive
plane diagrams with nodes and wires labelled by the generators of $\Sigma$.
The tensor product on words is given by concatenation, and on morphisms by the
tensor product of diagrams defined above; composition is given by vertical
stacking of diagrams.

Equipped with the interpretation $N \in [\Sigma, \mathbb{F}(\Sigma)]$ sending each
generator $d$ to the deformation class of the single-node diagram labelled by
$d$, the category $\mathbb{F}(\Sigma)$ is the \emph{free} monoidal category on
$\Sigma$ in the sense of \cref{def:free}.
\end{theorem}
This means we can use string diagrams to reason about morphisms in a monoidal category: whenever something holds in a free monoidal category, it holds in any monoidal category.

\paragraph{Equational extensions.}
The freeness theorem describes the morphisms generated with \emph{no} relations
beyond those of the monoidal structure itself. In applications one wants to
impose equations between morphisms---for instance the laws governing a functorial
box, or a naturality condition. Because the value of a diagram is purely a
function of its deformation class, one may quotient the diagrams by additional
equations that replace a layer of a diagram with another layer of the same domain
and codomain. As such a replacement is local, preserves the \emph{value}, and
does not alter the surrounding topology, the resulting graphical calculus is
sound and complete for the monoidal category \emph{presented} by the signature
together with those equations: that is, two diagrams are equal in the presented
category if and only if they are related by a finite sequence of deformations and
equational replacements. This is the principle underlying functorial
boxes~\cite{mellies_functorial_2006}, where a node is allowed to enclose an
entire sub-diagram, and various other extensions describing naturality and
similar conditions.

\begin{example}
Below is the equation stipulating the functoriality of boxes $F$ and $G$, and the
naturality condition expressed using the functorial boxes of
Melli\`es~\cite{mellies_functorial_2006} together with the natural-transformation
notation $\coevaltikz$ introduced by Ghica and
Zanasi~\cite{ghica-zanassi2023string}.
\[
\tikzfig{./figures/string/functorial_box_example}
\]
A functorial box is like a magnifying glass that lets you zoom in through the functor by delimiting a part of the diagram and relabelling the wires and nodes.
\end{example}

In order to apply such a replacement to a diagram $(\Gamma, v)$, it is necessary
to identify an \emph{incision}~\cite{joyal_geometry_II}: a region
$R = [\alpha,\beta] \times [c,d]$ whose horizontal sides
$[\alpha,\beta]\times\{c\}$ and $[\alpha,\beta]\times\{d\}$ meet no inner nodes of
$\Gamma$, and whose vertical sides $\{\alpha\}\times[c,d]$ and
$\{\beta\}\times[c,d]$ do not meet $\Gamma$ at all. After deforming the diagram
outside $R$ so that the full levels $x = c$ and $x = d$ contain no nodes, we
obtain a decomposition
\[
  v(\Gamma) = f \circ \big(h \otimes v(\Gamma \cap R) \otimes k\big) \circ g,
\]
where $f$ and $g$ are the values of the pieces above and below the region, and
$h$ and $k$ the values of the pieces to the left and right of it. 
A rewrite rule is a pair of diagrams $\langle(\Omega,w), (\Omega',w')\rangle$ with
the same domain and codomain and equal value, $w(\Omega) = w'(\Omega')$.
To apply it, we seek a \emph{match} of $\Omega$ into the incision: an embedding of
$\Omega$ into the region $R$ whose image is a deformation of $\Gamma \cap R$,
fixing the boundary, so that the wires meet the sides of $R$ at the same points
throughout the deformation. By deformation-invariance, such a match gives
$v(\Gamma \cap R) = w(\Omega)$. We then replace $\Gamma \cap R$ by $\Omega'$,
placing it via the boundary identification supplied by the match together with
$\mathrm{dom}\,\Omega = \mathrm{dom}\,\Omega'$ and
$\mathrm{cod}\,\Omega = \mathrm{cod}\,\Omega'$. Since the decomposition isolates
$v(\Gamma \cap R)$ as a single factor and $w(\Omega) = w'(\Omega')$, the resulting
diagram has the same value as $\Gamma$; the replacement is sound. In the symmetric
case, treated below, only an isomorphism of the underlying \emph{unanchored}
diagrams is required---the boundary order may be permuted, absorbed by the
symmetry---which is one of the simplifications the symmetric setting brings.
% \begin{remark}
% The presence of a symmetry $\sym_{A,B} : A \otimes B \to B \otimes A$ in $\mathcal{C}$
% has a profound effect on the picture above. 
% The crucial property is that the symmetry is self-inverse, $\sym_{A,B} = \sym_{B,A}^{-1}$. 
% When it is present, the planar embedding may be discarded entirely and deformation equivalence collapses to mere isomorphism of the underlying (still topological) graphs, with no reference to the plane. 
% Without self-inverseness the symmetry is a \emph{braiding}, and one must embed the graph in $\mathbb{R}^3$ to record which wire passes over which when two wires cross.\footnote{One may still regard the symmetric case as geometric: isomorphism of diagrams coincides with ambient isotopy in four dimensions, where any over-crossing may be slid past an under-crossing~\cite{Selinger_2010}.}
% While the notion of equivalence is simplified in the symmetric case, the valuation of a diagram is more complicated.
% We will recap the symmetric case next.
% \end{remark}

In the symmetric case, the graphs we consider are required to be \emph{polarised}, meaning that the wires from $\mathrm{in}(x)$ and $\mathrm{out}(x)$ are linearly ordered for each inner node $x$: there will be no embedding to induce the order, so it must come built-in.
The valuation of such a graph is then again given by layers but approached differently.
Suppose $\Gamma$ is a polarised graph with boundary and let $\mathring{\Gamma}$ be the set of inner nodes of $\Gamma$ with a partial order $\leq$ given by $x \leq y$ if there is a directed path from $x$ to $y$ in $\Gamma$.
Define a \emph{level} of $\Gamma$ to be a downward closed subset of $\mathring{\Gamma}$, i.e., a subset $L \subseteq \mathring{\Gamma}$ such that if $x \in L$ and $y \leq x$, then $y \in L$.
A wire $e$ is cut by a level $L$ if $\mathrm{src}(e) \in L \cup \mathrm{dom}\,\Gamma$ and $\mathrm{tgt}(e) \in (\mathring{\Gamma} \setminus L) \cup \mathrm{cod}\,\Gamma$.
We can similarly slice the graph between two levels $L_1$ and $L_2$ to yield a \emph{layer} $\Gamma[L_1, L_2]$ if $L_1 \subseteq L_2$ which is the graph with inner nodes $L_2 \setminus L_1$ with wires whose endpoints are either both in $L_2 \setminus L_1$ or cut by either of the levels.
$\mathrm{dom}\,\Gamma[L_1, L_2]$ is then the set of wires cut by $L_1$ and $\mathrm{cod}\,\Gamma[L_1, L_2]$ is the set of wires cut by $L_2$.

Before we can assign a value to a polarised graph we need to define a category whose objects are invariant under symmetry.
\begin{definition}[Chapter 2. Section 1.~\cite{joyal_geometry_1991}]
    Let $\mathcal{C}$ be a symmetric monoidal category.
    We define a category $\widetilde{\mathcal{C}}$ by the following data.
    \paragraph{Objects.} Objects are families $(A_s \vert s \in S)$ of objects of $\mathcal{C}$ indexed by finite sets $S$.
    \paragraph{Morphisms.} A morphism $(A_s \vert s \in S) \to (B_t \vert t \in T)$ is given by an equivalence class of triples $(\phi, f, \psi)$ consisting of linear orderings $\phi : [1 .. m] \to S$ and $\psi : [1 .. n] \to T$ and an arrow $f : \bigotimes_{i=1}^{m} A_{\phi(i)} \to \bigotimes_{j=1}^{n} B_{\psi(j)}$ in $\mathcal{C}$.
    A triple $(\phi, f, \psi)$ is equivalent to $(\phi', f', \psi')$ if the following diagram commutes
    % https://q.uiver.app/#q=
\[\begin{tikzcd}
	{\bigotimes_{i=1}^{m}A_{\phi(i)}} && {\bigotimes_{j=1}^{n}B_{\psi(j)}} \\
	{} \\
	{\bigotimes_{i=1}^{m}A_{\phi'(i)}} && {\bigotimes_{j=1}^{n}B_{\psi'(j)}}
	\arrow["f", from=1-1, to=1-3]
	\arrow["{\langle\phi'^{-1}\phi\rangle}"', from=1-1, to=3-1]
	\arrow["{\langle \psi'^{-1}\psi\rangle}", from=1-3, to=3-3]
	\arrow["{f'}"', from=3-1, to=3-3]
\end{tikzcd}\]
where $\langle \phi^{-1}\psi \rangle : \bigotimes_{i=1}^{m} X_{\psi(i)} \to \bigotimes_{i=1}^{m} X_{\phi(i)}$ is a symmetry isomorphism induced by a permutation $\phi^{-1} \circ \psi : [1..m] \to [1..m]$.
Given two (equivalence classes of) morphisms $[\phi_1, f, \psi_1]$ and $[\phi_2, g, \psi_2]$ their composition is given by picking representatives $(\phi_1, f, \psi_1)$ and $(\psi_1, g', \psi_2)$---that is, by re-representing the second morphism by using $\psi_1$ as its source ordering and such that $g' = g \circ \langle\phi_2^{-1}\psi_1\rangle$.
This choice is possible because every morphism admits a representative with any source ordering.
The resulting composition is then $[\phi_1, g' \circ f, \psi_2]$.

The category $\tilde{\mathcal{C}}$ is further symmetric monoidal. 
The tensor $\boxtimes$ on objects is given by $(A_s \vert s \in S) \boxtimes (B_t \vert t \in T) = (U_u \vert u \in S \sqcup T)$, where $U_{\mathrm{inj}_{S}(s)} = A_s, U_{\mathrm{inj}_{T}(t)} = B_t$, with symmetry inherited from $\mathcal{C}$ and with the evident action on morphisms.
The empty family is the unit.
\end{definition}
The value of a polarised graph $\Gamma$ is then
\[
\widetilde{v}(\Gamma) : (v_0(s) \vert s \in \mathrm{dom}\,\Gamma) \to (v_0(t) \vert t \in \mathrm{cod}\,\Gamma),
\]
where $v=(v_0,v_1)$ is the valuation.
That is, the value, formally defined later, of a polarised graph is a morphism in $\widetilde{\mathcal{C}}$ which is invariant under different orderings of the domain and codomain.
By fixing a linear order on the domain and codomain, we can assign a morphism in $\mathcal{C}$.

Before we define a value of a polarised graph, another category will be of use.
\begin{definition}[Chapter 2. Section 1.~\cite{joyal_geometry_1991}]
    Let $\mathcal{C}$ be a symmetric monoidal category and $I$ a finite set.
    The category $\mathrm{sp}(I, \mathcal{C})$ is given by the following data.
    \paragraph{Objects.} An object is a family $(A_s \mid s \in S \xrightarrow{\phi} I)$,
    where $(A_s \mid s \in S)$ is a family of objects of $\mathcal{C}$ indexed by a
    finite set $S$, and $\phi : S \to I$ is a map such that each pre-image $\phi^{-1}(i)$
    is linearly ordered.
    \paragraph{Morphisms.} A morphism
    $(A_s \mid s \in S \xrightarrow{\phi} I) \to (B_t \mid t \in T \xrightarrow{\psi} I)$
    is a family of maps
    \[
      \Big(u_i : \bigotimes_{\phi(s) = i} A_s \to \bigotimes_{\psi(t) = i} B_t \;\Big\vert\; i \in I\Big),
    \]
    one for each $i \in I$, with composition taken componentwise.
\end{definition}
There is a functor $\widetilde{\bigotimes}_{i \in I} : \mathrm{sp}(I, \mathcal{C}) \to \widetilde{\mathcal{C}}$
sending an object $(A_s \mid s \in S \xrightarrow{\phi} I)$ to the underlying family
$(A_s \mid s \in S)$, and a morphism $(u_i \mid i \in I)$ to $\widetilde{\bigotimes}_{i \in I} u_i$,
defined as follows. Choose a linear order on $I$; together with the pre-image orders this
determines linear orders $\omega_S : [1,m] \xrightarrow{\sim} S$ and
$\omega_T : [1,n] \xrightarrow{\sim} T$ by first ordering by $I$, then within each pre-image $\phi^{-1}(i)$ and $\psi^{-1}(i)$ by their given order.
Form the genuine
$\mathcal{C}$-morphism
\[
  f \;=\; \bigotimes_{i \in I} u_i \;:\;
  \bigotimes_{i \in I} \bigotimes_{\phi(s) = i} A_s \;\longrightarrow\;
  \bigotimes_{i \in I} \bigotimes_{\psi(t) = i} B_t .
\]
The arrow $\widetilde{\bigotimes}_{i \in I} u_i$ in $\widetilde{\mathcal{C}}$ is then the
map represented by the triple $(\omega_S, f, \omega_T)$. Different choices of
linear order on $I$ yield different representatives of this kind, but all lie in the
same equivalence class, so the functor is well-defined.
Put simply, the objects are collections sorted into ordered groups, with no order imposed on the index set 
$I$ itself, and the morphisms are families of maps between corresponding groups.

This machinery is necessary because when we slice a polarised graph, we do not know the order of the connected components of the slice, so the value must be order-invariant.
For instance, a slice with two components 
$f$ and $g$ may be read with $f$ before $g$ or with $g$ before $f$; the two readings order the boundary wires oppositely and are related by a symmetry, so they must yield the same value. Only the inputs and outputs of each individual component are intrinsically ordered, by the polarisation, which is precisely the data recorded by $\mathrm{sp}(I,\mathcal{C})$,
where $I$ is the unordered set of components.

We can now give a value to a polarised graph $\Gamma$ by slicing it into \emph{elementary} components.
A graph $\Gamma$ is called \emph{elementary} if its corresponding $\mathring{\Gamma}$ is a discrete partially ordered set, i.e., there are no distinct inner nodes $x,y$ such that $x \leq y$.
Let $I = \pi_0(\Gamma)$ be the set of connected components of $\Gamma$. 
Then for each $i \in I$, its value $u_i$ is $v_1(i)$ if $i$ is a single node and $\id_{v_0(i)}$ if $i$ is a single wire. 
Then, each elementary graph $\Gamma$ is evaluated as a morphism in $\widetilde{\mathcal{C}}$ by setting:
\[
\widetilde{v}(\Gamma) = \widetilde{\bigotimes}_{i \in I} u_i ,
\]
with $(u_i \vert i \in I) : (v_0(s) \vert s \in \mathrm{dom}\,\Gamma \to I) \to (v_0(t) \vert t \in \mathrm{cod}\,\Gamma \to I)$.

\begin{example}
Below is an elementary polarised graph, since the nodes $f$ and $g$ are
incomparable in the partial order of $\mathring{\Gamma}$. The polarisation order
is left-to-right.
\[
\tikzfig{./figures/string/polarised_example}
\]
The two connected components give a morphism $(u_1, u_2)$ in
$\mathrm{sp}(I, \mathcal{C})$, where $I = \{1, 2\}$ and $u_1 = f$, $u_2 = g$ are
the morphisms labelling them. Choosing the order $1 < 2$ on $I$ produces the
representative
\[
  u_1 \otimes u_2 \;:\; A_1 \otimes A_2 \otimes A_3 \otimes C
  \;\longrightarrow\; B \otimes D_1 \otimes D_2
\]
in $\mathcal{C}$. The value $\widetilde{v}(\Gamma)$ is the arrow of $\widetilde{\mathcal{C}}$
from $(A_1, A_2, A_3, C)$ to $(B, D_1, D_2)$ that this morphism represents. The
opposite order $2 < 1$ gives instead $g \otimes f$ on the reordered boundary, but
represents the same arrow of $\widetilde{\mathcal{C}}$, so $\widetilde{v}(\Gamma)$ is
well-defined.
\end{example}

For any progressive polarised graph $\Gamma$, pick a maximal chain $\varnothing \subset L_1 \subset \ldots \subset L_n = \mathring{\Gamma}$ of levels.
Each layer $\Gamma[L_i, L_{i+1}]$ has precisely one inner node and is therefore elementary.
Define $\widetilde{v}(\Gamma) = \widetilde{v}(\Gamma[L_{n-1}, L_n]) \circ \ldots \circ \widetilde{v}(\Gamma[\varnothing, L_1])$.

\begin{example}\label{example:layering_polarised_graph}
Below is an example of layering of a polarised graph.
\[
\tikzfig{./figures/string/polarised_example_2}
\]
The levels are given by $\varnothing \subset \{f\} \subset \{f,g\} \subset \{f,g,h\} = \mathring{\Gamma}$.
$\Gamma[\varnothing, \{f\}]$ is an elementary graph containing the node $f$, its input wires and output wires, and the wire with target in $g$, but does not contain the node $g$ itself: in total, there are two connected components and valuation is $\{f,\id_{C}\}$.
$\Gamma[\{f\}, \{f,g\}]$ is an elementary graph containing the node $g$, its input wires and output wires, and the output wire of $f$: the valuation is $\{\id_{A}, g\}$.
Finally, $\Gamma[\{f,g\}, \{f,g,h\}]$ is an elementary graph containing the node $h$, its input wires and output wires, and the output wires of $g$: the valuation is $\{h, \id_{D_1 \otimes D_2}\}$.

To obtain the value of the entire graph, we compose the three layer-values.
First, within each elementary layer, the components are bundled into a single
arrow of $\widetilde{\mathcal{C}}$ by the functor 
$\widetilde{\bigotimes}_{i \in I}$:
\begin{align*}
  \widetilde{v}_1 &= \widetilde{\bigotimes}(\, f,\ \mathrm{id}_C \,)
    : (A_1, A_2, A_3, C) \to (A, C), \\
  \widetilde{v}_2 &= \widetilde{\bigotimes}(\, \mathrm{id}_A,\ g \,)
    : (A, C) \to (A, D_1, D_2), \\
  \widetilde{v}_3 &= \widetilde{\bigotimes}(\, h,\ \mathrm{id}_{D_1 \otimes D_2} \,)
    : (A, D_1, D_2) \to (B, D_1, D_2),
\end{align*}
where each interface family is written unordered. 
The value of the whole graph is therefore the composite in $\widetilde{\mathcal{C}}$, read bottom to top:
\[
  \widetilde{v}(\Gamma) = \widetilde{v}_3 \circ \widetilde{v}_2 \circ \widetilde{v}_1
  : (A_1, A_2, A_3, C) \to (B, D_1, D_2).
\]
\end{example}

As before, a diagram is a graph coupled with a valuation.
An equivalence of diagrams is then captured by the notion of \emph{isomorphism}.
\begin{definition}
An isomorphism $f : \Gamma \to \Omega$ of progressive polarised diagrams is an isomorphism of the underlying graphs with boundary which preserves orientation and the orders on each input and output, and is compatible with the valuations. 
\end{definition}
\begin{theorem}\label{theorem:symm_iso_preserve_value}
If $\Gamma \cong \Omega$, then $\widetilde{v}(\Gamma) = \widetilde{v}(\Omega)$.
\end{theorem}
A graph $\Gamma$ is called \emph{anchored} if there is an ordering imposed on the $\mathrm{dom}\,\Gamma$ and $\mathrm{cod}\,\Gamma$.
In this case the valuation of $\Gamma$ is a morphism in $\mathcal{C}$, by picking the ordering on $(v(s) \vert s \in \mathrm{dom}\,\Gamma)$ and $(v(t) \vert t \in \mathrm{cod}\,\Gamma)$.

\begin{remark}
Because progressive polarised diagrams are not embedded in the plane and are identified up to isomorphism, drawing them is strictly speaking ambiguous.
For example, we could have drawn the diagram in~\cref{example:layering_polarised_graph} as
\[
\tikzfig{./figures/string/polarised_example_3},
\]
or we could have crossed some wires in between, without changing the connectivity.
It is therefore a convention to draw a diagram with its domain and codomain ordered
left to right according to the anchoring, letting the symmetry be expressed by crossed
wires.
It is then common to \enquote{untangle} wires that cross twice, as this does not change
the connectivity (the symmetry being self-inverse, $\sym_{A,B} \circ \sym_{B,A} = \id$).
In contrast, the equivalence of diagrams for \emph{braided} monoidal categories is
defined up to isotopy in $\mathbb{R}^{3}$, the extra dimension recording which wire passes
over which at a crossing.
One may likewise present the symmetric case geometrically, as equivalence up to isotopy
in $\mathbb{R}^{4}$, viewing a drawn diagram as a $2$-dimensional projection of a diagram
embedded in $\mathbb{R}^{4}$.
This notion of equivalence coincides with the isomorphism of progressive polarised
diagrams; see the survey of Selinger~\cite{Selinger_2010} for more details.
\end{remark}
\begin{example}
Recall the diagram from~\cref{example:layering_polarised_graph}.
Suppose the anchoring is given by ordering $A_1 < A_2 < A_3 < C$ on the domain and $D_1 < D_2 < B$ on the codomain.
Then, the value of the diagram, $v(\Gamma)$, is given by a morphism $u : A_1 \otimes A_2 \otimes A_3 \otimes C \to D_1 \otimes D_2 \otimes B$ in $\mathcal{C}$.
Thanks to evaluating the layers in $\widetilde{\mathcal{C}}$, the value $v(\Gamma)$ is invariant under different choices of ordering within each layer.
Suppose we ordered the layers as $v_1 = \id_C \otimes f$, $v_2 = \id_A \otimes g$, and $v_3 = \id_{D_1 \otimes D_2} \otimes h$.
The value of the diagram is then given by the composite
\begin{align*}
A_1 \otimes A_2 \otimes A_3 \otimes C &\xrightarrow{\sym_{A_1 \otimes A_2 \otimes A_3, C}} C \otimes A_1 \otimes A_2 \otimes A_3\\
&\xrightarrow{v_1} C \otimes A\\
&\xrightarrow{\sym_{C,A}} A \otimes C\\
&\xrightarrow{v_2} A \otimes D_1 \otimes D_2\\
&\xrightarrow{\sym_{A, D_1 \otimes D_2}} D_1 \otimes D_2 \otimes A \xrightarrow{v_3} D_1 \otimes D_2 \otimes B .
\end{align*} 
This is the same morphism had we chosen any other ordering, for example, the left-to-right ordering of $v_1 = f \otimes \id_C$, $v_2 = \id_{A} \otimes g$, and $v_3 = h \otimes \id_{D_1 \otimes D_2}$, which is
\begin{align*}
A_1 \otimes A_2 \otimes A_3 \otimes C &\xrightarrow{v_1} A \otimes C\\
&\xrightarrow{v_2} A \otimes D_1 \otimes D_2\\
&\xrightarrow{v_3} B \otimes D_1 \otimes D_2\\
&\xrightarrow{\sym_{B, D_1 \otimes D_2}} D_1 \otimes D_2 \otimes B.
\end{align*}
\end{example}

Finally, anchored diagrams can be equipped with a structure of a symmetric monoidal category.
The tensor product of two diagrams is given by their disjoint union, while the composition of two anchored progressive polarised diagrams $\Gamma$ and $\Omega$ such that $\mathrm{cod}\,\Gamma = \mathrm{dom}\,\Omega$ is given by the disjoint union of the two graphs, identifying wires with target in $\mathrm{cod}\,\Gamma$ with wires with source in $\mathrm{dom}\,\Omega$.
Given a monoidal signature $\Sigma$, a free symmetric monoidal category $\mathbf{F}(\Sigma)$ is given by the following data.
\paragraph{Objects.} The objects are words of $\Sigma_0$.
\paragraph{Morphisms.} The morphisms are isomorphism classes of anchored progressive polarised diagrams.
Composition and tensor product are given as described above.
The symmetry $\sym_{V,W} : VW \to WV$ for words $V = V_1 \ldots V_n$ and $W = W_1 \ldots W_m$ is given by the anchored diagram $\Gamma$ with $n + m$ wires, represented by disjoint closed intervals and with a corresponding valuation, such that $\mathrm{dom}\,\Gamma$ is ordered $V_1 < \ldots < V_n < W_1 < \ldots < W_m$ and $\mathrm{cod}\,\Gamma$ ordered $W_1 < \ldots < W_m < V_1 < \ldots < V_n$.
\begin{theorem}
The data above define a free symmetric monoidal category on the signature $\Sigma$ in the same sense as \cref{theorem:free_string_planar}.
\end{theorem}

\paragraph{Diagram replacement.}
While the presence of a symmetry greatly simplifies the notion of sameness for the diagrams, it makes the formulation of rewriting or replacing of sub-diagrams more subtle as there is no longer a planar embedding to rely on to identify a square.
    
Let $\langle (\Omega, w), (\Omega', w') \rangle$ be a rewrite rule: a pair of
anchored diagrams whose domain words agree and whose codomain words agree, that
is $\mathrm{dom}\,\Omega = \mathrm{dom}\,\Omega'$ and
$\mathrm{cod}\,\Omega = \mathrm{cod}\,\Omega'$ as words in $\Sigma_0$, imposed as
the equation $\widetilde{w}(\Omega) = \widetilde{w'}(\Omega')$. Given an anchored
diagram $(\Gamma, v)$, we identify a \emph{convex} sub-diagram $\Gamma'$ of
$\Gamma$---a sub-diagram whose nodes are closed under directed paths in
$\Gamma$ between them---equipped with an isomorphism $\Gamma' \cong \Omega$ of
unanchored diagrams.

We replace $\Gamma'$ with $\Omega'$ to form $\Gamma''$: we remove the nodes and
wires of $\Gamma'$ and insert $\Omega'$, identifying each boundary wire of
$\Gamma'$ with a boundary wire of $\Omega'$ along the composite bijection
\[
  \mathrm{dom}\,\Gamma' \xrightarrow{\ \cong\ } \mathrm{dom}\,\Omega
  \xrightarrow{\ \cong\ } \mathrm{dom}\,\Omega',
\]
where the first map is the action of $\Gamma' \cong \Omega$ on boundary wires and
the second matches wires by position---well-defined since
$\mathrm{dom}\,\Omega = \mathrm{dom}\,\Omega'$ as words---and similarly for the
codomains. The rest of $\Gamma$ is left untouched.

By convexity, the nodes of $\Gamma'$ are exactly $L_2 \setminus L_1$ for the
levels $L_1 = \{x : x < \Gamma'\}$ and $L_2 = L_1 \cup \mathring{\Gamma'}$, so
$\Gamma'$ occupies the layer $\Gamma[L_1, L_2]$. Furthermore, this layer is the disjoint union of $\Gamma'$ and the wires passing
through it, each cut by both levels. Since $\widetilde{v}$ sends disjoint unions
to the monoidal product $\boxtimes$ of $\widetilde{\mathcal{C}}$, the layer value
decomposes as
\[
  \widetilde{v}(\Gamma[L_1, L_2])
  = \widetilde{v}(\Gamma') \boxtimes \widetilde{\bigotimes}_{i \in I} u_i,
\]
where $I = \pi_0\big(\Gamma[L_1, L_2] \setminus \Gamma'\big)$ indexes the
pass-through wires and each $u_i = \mathrm{id}_{v_0(e_i)}$ is the identity on its
wire's object. Slicing $\Gamma$ at $L_1$ and $L_2$ factors its value through the
layer,
\[
  \widetilde{v}(\Gamma)
  = \widetilde{v}(\Gamma[L_2, \mathring{\Gamma}])
    \circ \widetilde{v}(\Gamma[L_1, L_2])
    \circ \widetilde{v}(\Gamma[\varnothing, L_1]).
\]
Since $\Gamma' \cong \Omega$ as unanchored diagrams,
$\widetilde{v}(\Gamma') = \widetilde{w}(\Omega)$ by \cref{theorem:symm_iso_preserve_value},
and $\widetilde{w}(\Omega) = \widetilde{w'}(\Omega')$ by the rule. The replacement
alters only the factor $\widetilde{v}(\Gamma')$ in the decomposition above,
leaving the pass-through wires and the surrounding layers untouched; substituting
the equal value $\widetilde{w'}(\Omega')$ gives
$\widetilde{v}(\Gamma'') = \widetilde{v}(\Gamma)$. The replacement is therefore
sound.

\begin{remark}
To the best of the author's knowledge, the formulation above represents a folklore result in the theory of string diagrams, and is not explicitly stated in the literature.
This is unlike the planar case, where the notion of slicing and replacing can be traced to the other work by Joyal and Street~\cite{joyal_geometry_II}.
This is perhaps unsurprising, as the topological nature of these diagrams makes the rewriting and reasoning about the rewriting intricate in both cases.
While the planar case formalises well the pen-and-paper reasoning using string diagrams, mechanisation of this process is difficult as the notion of equivalence of diagrams is not trivial and requires one to check if one embedding is a continuous deformation of another.
In the symmetric case, the notion of equivalence is much simpler, but as we saw, the notion of slicing and replacing is more subtle.
This inspired a separate line of work by Bonchi et. al.~\cite{bonchi_string_2022-1,bonchi_string_2022-2,bonchi_string_2022-3,zanassi_comonoid} who treated string diagrams as mere graphical notation for morphism terms rather than as topological first-class objects, formalising them combinatorially as hypergraphs and formalising the rewriting of diagrams as double-pushout rewriting of hypergraphs.
The approach keeps the simplicity of the notion of equivalence of diagrams and brings the convenience of double-pushout rewriting which is an established technique for graph transformations by treating the \emph{graphs} as first-class objects rather than something induced by points in a topological space.
We review this approach next.
\end{remark}

\section{String diagrams 2.0}
\label{sec:string-diagrams-Bonchi}

Another approach to string diagrams is to define them syntactically, starting from the morphism terms first and treating the diagrams as a graphical representation for the latter.
More precisely, string diagrams are defined as equivalence classes of $\Sigma$-terms over a monoidal signature $\Sigma$, modulo the axioms of a symmetric monoidal category.
Before, we had string diagrams as first-class objects paired with a valuation in $\Sigma$, which induced a symmetric monoidal category on the diagrams themselves.
Now we will consider free symmetric monoidal categories generated by $\Sigma$-terms, and string diagrams as a graphical representation of the morphisms in the free category.
\begin{definition}[$\Sigma$-terms]
  \label{def:sigma-terms}
  Given a monoidal signature $\Sigma$ of~\cref{def:mon_signature}, a set of $\Sigma$-terms $\operatorname{Tm}_{\Sigma}$ is defined by the following grammar:
  \[
  t \Coloneqq f \in \Sigma_1 \;\vert\; \id_{\I} \;\vert\; \id_A,\; A \in \Sigma_0 \;\vert\; \sym_{A,B},\; A,B \in \Sigma_0 \;\vert\; t_1 \otimes t_2 \;\vert\; t_1 \circ t_2.
  \] 
  We will also write $(f \fatsemi g)$ for $g \circ f$---a composition in diagrammatic order.
\end{definition}

\begin{definition}
  Given a $\Sigma$-term $t$, its domain ($\mathrm{dom}\;t$) and codomain ($\mathrm{cod}\;t$) are lists defined as follows:
  \[
  \mathrm{dom}\;t = \begin{cases}
    \mathrm{dom}\;f & \text{if } t = f,\;f \in \Sigma_{1}\\
    [] & \text{if } t = \id_{\I}\\
    [A] & \text{if } t = \id_A \\
    [A,B] & \text{if } t = \sym_{A,B} \\
    \mathrm{dom}\;t_1 \mathbin{+\!+} \mathrm{dom}\;t_2 & \text{if } t = t_1 \otimes t_2\\
    \mathrm{dom}\;t_2 & \text{if } t = t_1 \circ t_2
  \end{cases}
  \]
  \[
  \mathrm{cod}\;t = \begin{cases}
    \mathrm{cod}\;f & \text{if } t = f,\;f \in \Sigma_{1}\\
    [] & \text{if } t = \id_{\I}\\
    [A] & \text{if } t = \id_A \\
    [B,A] & \text{if } t = \sym_{A,B} \\
    \mathrm{cod}\;t_1 \mathbin{+\!+} \mathrm{cod}\;t_2 & \text{if } t = t_1 \otimes t_2\\
    \mathrm{cod}\;t_1 & \text{if } t = t_1 \circ t_2
  \end{cases}~.
  \]
  We will limit our attention to well-typed $\Sigma$-terms, the terms for which $t_1 \circ t_2$ is defined, that is, $\mathrm{dom}\;t_1 = \mathrm{cod}\;t_2$.
\end{definition}
This is a generalisation of a free $\Sigma$-algebra that we had in~\cref{def:free_sigma_algebra}.
\begin{definition}[Symmetric monoidal theory]
  A (presentation of) symmetric monoidal theory (SMT, for short) is a pair $(\Sigma, \mathcal{E})$, given by a monoidal signature $\Sigma$ and a set of equations $\mathcal{E}$ between $\Sigma$-terms with the same domain and codomain.
\end{definition}

String diagrams for $\Sigma$-terms can then be given by the set generated by the following grammar, simplified for the case of $\Sigma_0 \cong \{*\}$, such that every wire carries the same type, quotiented by the axioms in~\cref{fig:string-diagram-axioms}.
\begin{figure}[h!]
    \centering
    \tikzfig{./figures/introduction/string-diagrams-syntax}
    \caption{String diagram syntax for symmetric monoidal categories; dashes represent multiple wires.}
    \label{fig:string-diagram-syntax-2}
\end{figure}

\begin{figure}
\begin{align*}
  (s \fatsemi t) \fatsemi u \equiv s \fatsemi (t \fatsemi u) &\quad \id_n \fatsemi s \equiv s \equiv s \fatsemi \id_m\\
  (s \otimes t) \otimes u \equiv s \otimes (t \otimes u) &\quad \id_{\I} \otimes s \equiv s \equiv s \otimes \id_{\I}\\
  (s \otimes t) \fatsemi (u \otimes v) \equiv (s \fatsemi u) \otimes (t \fatsemi v) &\quad \id_m \otimes \id_n \equiv \id_{m+n}\\
  (\sym_{m,n} \otimes \id_o) \fatsemi (\id_n \otimes \sym_{m,o}) \equiv \sym_{m,n+o} &\quad \sym_{n,m} \fatsemi \sym_{m,n} \equiv \id_{n+m}\\
  (s \otimes \id_m) \fatsemi \sym_{n,m} &\equiv \sym_{o,m} \fatsemi (\id_m \otimes s)
\end{align*}
\caption{Equivalences of $\Sigma$-terms, generating the same string diagram~\cite{bonchi_string_2022-1}.}
\label{fig:string-diagram-axioms}
\end{figure}

\begin{remark}
Although the syntactic diagrams of this section and the polarised diagrams of Joyal and Street are different kinds of object, both constructions yield the \emph{free} strict symmetric monoidal category on $\Sigma$.
By the universal property of freeness this category is unique up to equivalence; under this equivalence, isomorphism of Joyal--Street diagrams corresponds exactly to SMC-axiom-equality of $\Sigma$-terms.
The syntactic and topological presentations are thus two presentations of the same free category.

In this syntactic setting, we can also recover some topological properties similar to those of Joyal--Street planar diagrams.
For example, we can slide boxes along a single wire, which results in the same diagram since it corresponds to a sequential composition with identity morphism; we can similarly slide boxes past boxes on other wires which is justified by the bifunctoriality equation; we can untangle wires that cross twice, which is validated by $\sym$ being an involution; we can also slide boxes along crossed wires, which is validated by the naturality of $\sym$.
These manipulations may seem topological, but they are completely governed by the equations on the syntax with no real topology involved, unlike in the case of the diagrams by Joyal and Street.

An immediate benefit of this syntactic approach, is that any extension to string-diagrammatic syntax is automatically well-defined: it is just a graphical rendering of the corresponding term construction governed by the same equations.
This is the approach that we commit to in this thesis.
\end{remark}

$\Sigma$-terms induce a free syntactic symmetric monoidal category which is an instance of a collection of categories called $\mathrm{PROPs}$, short for \textbf{PRO}duct and \textbf{P}ermutation categories.
\begin{definition}
  A $\mathrm{PROP}$ is a symmetric strict monoidal category whose objects are natural numbers, with the tensor product on objects given by addition.
\end{definition}
This is a generalisation of Lawvere theories, as seen in~\cref{chapter:algebras}.

\begin{example}\label{example:initial_prop}
Let $\mathbf{P}$ be the category whose objects are natural numbers and tensor product given by addition.
Let $\mathbf{P}(m,n) = \varnothing$ if $m \neq n$ and $\mathbf{P}(n,n) = S_n$, the symmetric group on $n$ elements, with composition given by group multiplication.
Then $\mathbf{P}$ is a $\mathrm{PROP}$, called the \emph{permutation} $\mathrm{PROP}$, as every morphism is a permutation on a finite set.
\end{example}
\begin{definition}
\label{def:coloured-prop}
For a set $\mathcal{C}$, a \emph{coloured} $\mathrm{PROP}$ is a symmetric strict monoidal category whose objects are words in $\mathcal{C}$, with the tensor product on objects given by concatenation.
\end{definition}
This is a generalisation of Lawvere theories in the multi-sorted case.
A $\mathrm{PROP}$ is a coloured $\mathrm{PROP}$ with a single colour.

\begin{definition}
Let $\mathbf{S}_{\Sigma,\mathcal{E}}$ be a free syntactic PROP presented by SMT $(\Sigma, \mathcal{E})$ with $\Sigma_0 \cong \{*\}$, that is hom-sets $\mathbf{S}_{\Sigma,\mathcal{E}}(m,n)$ are sets of $\Sigma$-terms with domain $m$ and codomain $n$, quotiented by the equations of a symmetric monoidal category from~\cref{fig:string-diagram-axioms} and the equations in $\mathcal{E}$.
We also let $\mathbf{S}_{\Sigma}$ be the free syntactic PROP presented by the SMT $(\Sigma, \varnothing)$.
\end{definition}

\begin{remark}
Coloured $\mathrm{PROP}$s are presented by multi-sorted SMTs, where $\Sigma_0$ defines the set of colours or types.
\end{remark}
This is an analogue of a collection of free algebras $T_{\Sigma, \mathcal{E}}$, viewed as a Lawvere theory.
We already saw an instance of such a syntactic PROP in~\cref{example:comonoids}, another example is the theory of bialgebras that has both monoid and comonoid structure, interacting in a certain way.
\begin{example}
Consider a signature
\[
\Sigma_{\mathbb{B}} = \{\,\delta : 1 \to 2,\;\; \varepsilon : 1 \to 0,\;\;
\mu : 2 \to 1,\;\; \eta : 0 \to 1\,\}
\]
and equations $\mathcal{E}_{\mathbb{B}}$, grouped into the comonoid laws,
the monoid laws, and their interaction:
\begin{align}
  \delta \fatsemi (\delta \otimes \id_1)
    &\equiv \delta \fatsemi (\id_1 \otimes \delta)
    && \text{(coassociativity)} \label{eq:bialg-coassoc}\\
  \delta \fatsemi (\varepsilon \otimes \id_1)
    &\equiv \id_1
    && \text{(counitality)} \label{eq:bialg-counit}\\
  \delta \fatsemi \sym_{1,1}
    &\equiv \delta
    && \text{(cocommutativity)} \label{eq:bialg-cocomm}\\[2pt]
  (\mu \otimes \id_1) \fatsemi \mu
    &\equiv (\id_1 \otimes \mu) \fatsemi \mu
    && \text{(associativity)} \label{eq:bialg-assoc}\\
  (\eta \otimes \id_1) \fatsemi \mu
    &\equiv \id_1
    && \text{(unitality)} \label{eq:bialg-unit}\\
  \sym_{1,1} \fatsemi \mu
    &\equiv \mu
    && \text{(commutativity)} \label{eq:bialg-comm}\\[2pt]
  \mu \fatsemi \delta
    &\equiv (\delta \otimes \delta) \fatsemi
      (\id_1 \otimes \sym_{1,1} \otimes \id_1) \fatsemi (\mu \otimes \mu)
    && \text{(\textsc{b}1)} \label{eq:bialg-b1}\\
  \mu \fatsemi \varepsilon
    &\equiv \varepsilon \otimes \varepsilon
    && \text{(\textsc{b}2)} \label{eq:bialg-b2}\\
  \eta \fatsemi \delta
    &\equiv \eta \otimes \eta
    && \text{(\textsc{b}3)} \label{eq:bialg-b3}\\
  \eta \fatsemi \varepsilon
    &\equiv \id_0
    && \text{(\textsc{b}4)} \label{eq:bialg-b4}
\end{align}
This gives a presentation of the theory of bialgebras
$\mathbf{S}_{\Sigma_{\mathbb{B}}, \mathcal{E}_{\mathbb{B}}}$: a theory in
which resources can be both duplicated and merged, with a prescribed
interaction between the two operations.

A canonical model of this PROP is the category $\catname{B}$ with objects
the natural numbers and morphisms $\catname{B}(m,n)$ the $n \times m$
matrices over $\mathbb{N}$, with composition given by matrix
multiplication and tensor product by direct sum of matrices. The
generators are interpreted as
\[
\delta \mapsto \begin{pmatrix}1\\1\end{pmatrix},
\qquad
\varepsilon \mapsto \begin{pmatrix}\;\end{pmatrix},
\qquad
\mu \mapsto \begin{pmatrix}1 & 1\end{pmatrix},
\qquad
\eta \mapsto \begin{pmatrix}\;\end{pmatrix},
\]
the $2\times 1$, $0 \times 1$, $1 \times 2$, and $1 \times 0$ matrices
respectively (the second and the fourth being the empty matrices). One checks that
the equations of $\mathcal{E}_{\mathbb{B}}$ hold under this
interpretation, so it is a model of the theory of bialgebras; in fact
$\catname{B}$ is also complete, meaning that interpretation is an isomorphism $\mathbf{S}_{\Sigma_{\mathbb{B}},
\mathcal{E}_{\mathbb{B}}} \cong \catname{B}$~\cite{lackDedicatedAurelioCarboni}.

It is often more convenient to represent both the generators and the
equations graphically as string diagrams, as in
\cref{fig:bialgebra_generators_equations}.
\end{example}

\begin{figure}
\begin{subfigure}{\textwidth}
    \centering
    \tikzfig{./figures/string/bialgebra_generators}
    \subcaption{Bialgebra generators.}
\end{subfigure}
\begin{subfigure}{\textwidth}
    \centering
    \tikzfig{./figures/string/bialgebra_equations}
    \subcaption{Bialgebra equations.}
\end{subfigure}
\caption{String diagrams for the theory of bialgebras.}
\label{fig:bialgebra_generators_equations}
\end{figure}

Other examples of $\mathrm{PROPs}$ include \emph{Frobenius algebras}~\cite{kockFrobeniusAlgebras2D2003}, \emph{Hopf algebras}~\cite{bonchiInteractingHopfAlgebras2017}, and generally theories that have generators with multiple outputs which are related in some way and hence can not be projected and processed separately, for example, maps that return a joint probability distribution of their inputs~\cite{MarkovProp}.
% Another important example are theories that have commutative comonoid structure, that is also natural.
Another important class of PROPs consists of those equipped with a natural commutative comonoid structure.

\begin{theorem}[Fox~\cite{foxCoalgebrasCartesianCategories1976}]
\label{theorem:fox}
A symmetric monoidal category is cartesian precisely when every object \(X\) carries a coherent natural commutative comonoid structure
\[
\delta_X : X \to X \otimes X,
\qquad
\varepsilon_X : X \to \I.
\]
In this case, \(\otimes\) is the categorical product, \(\delta_X\) is the diagonal, and \(\varepsilon_X\) is the unique map to the terminal object.
\end{theorem}

\begin{example}[Cartesian PROPs and Lawvere theories]
\label{ex:cartesian-prop}
By \cref{theorem:fox}, a PROP equipped with such natural copy and discard operations is cartesian.
Since its objects are the natural numbers, with \(n = 1^{\otimes n}\), every object is a finite power of \(1\); hence the PROP is a Lawvere theory.
Conversely, every Lawvere theory is a cartesian PROP whose diagonals and terminal maps provide this natural commutative comonoid structure.
Thus, cartesian PROPs and Lawvere theories are the same structures viewed from monoidal and finite-product perspectives, respectively.
\end{example}
Therefore, PROPs are indeed generalisations of algebraic theopries.
% \begin{example}[Cartesian PROP $=$ Lawvere theory]
% \label{ex:cartesian-prop}
% Let $\Sigma$ contain, besides any other generators, a comonoid
% $\delta : 1 \to 2$ and $\varepsilon : 1 \to 0$, and let $\mathcal{E}$
% impose coassociativity, counitality, and cocommutativity, together with
% a \emph{naturality} requirement on each remaining generator: for every
% $f \in \Sigma_1$ with $f : m \to n$,
% \[
% f \fatsemi (\underbrace{\delta \otimes \cdots \otimes \delta}_{n})
% =
% (\underbrace{\delta \otimes \cdots \otimes \delta}_{m}) \fatsemi
% \sigma \fatsemi (f \otimes f),
% \qquad
% f \fatsemi (\underbrace{\varepsilon \otimes \cdots \otimes \varepsilon}_{n})
% = \underbrace{\varepsilon \otimes \cdots \otimes \varepsilon}_{m},
% \]
% where $\sigma$ is the canonical permutation interleaving the two copies
% of the $m$ inputs. The first equation says $f$ commutes with copying---its
% outputs may be copied before or after applying $f$---and the second that
% $f$ commutes with discarding. Together they make each generator a
% \emph{comonoid homomorphism}.

% It suffices to impose these equations on the generators. Comonoid
% homomorphisms are closed under composition and tensor: if $F$ and $G$
% each commute with $\delta$ and $\varepsilon$, then so do $F \fatsemi G$
% and $F \otimes G$, the requisite interleaving permutations composing by
% coherence of the symmetry. By induction on the structure of
% $\Sigma$-terms, \emph{every} morphism $F : m \to n$ of
% $\mathbf{S}_{\Sigma,\mathcal{E}}$ is therefore a comonoid homomorphism;
% the structural generators $\id$, $\sym$, $\delta$, $\varepsilon$ are
% homomorphisms already, by the comonoid laws and naturality of the
% symmetry. The comonoid $(\delta, \varepsilon)$ is thus natural in every
% morphism.

% By Fox's theorem, a symmetric monoidal category in which every object
% carries a natural commutative comonoid structure is \emph{cartesian}:
% the counit $U : (\mathbf{S}_{\Sigma,\mathcal{E}})^{\mathrm{C}} \to
% \mathbf{S}_{\Sigma,\mathcal{E}}$ is an isomorphism, so $\otimes$ is the
% categorical product, $\delta$ is the diagonal, and $\varepsilon$ is the
% unique map to the terminal object $0$. Concretely, naturality lets every
% diagram be rewritten so that copying and discarding are pushed back to
% the inputs: each generator may freely duplicate or delete its arguments,
% and a morphism $m \to n$ is determined by $n$ terms over $m$ copyable,
% discardable inputs.

% This is exactly a Lawvere theory. Once copying and discarding are free
% and natural, a morphism $m \to 1$ is an $m$-ary operation that may use
% each of its $m$ inputs any number of times---including zero---so
% \[
% \mathbf{S}_{\Sigma,\mathcal{E}}(m, 1) \;\cong\; T_{\Sigma'}(m)/\bar R,
% \]
% the $m$-ary terms of the corresponding algebraic theory modulo its
% equations, while the product structure forces the multi-output collapse
% \[
% \mathbf{S}_{\Sigma,\mathcal{E}}(m,n) \;\cong\;
% \mathbf{S}_{\Sigma,\mathcal{E}}(m,1)^{\,n}
% \]
% characteristic of the cartesian case. Here $\Sigma' = \Sigma \setminus
% \{\delta, \varepsilon\}$ is the original signature with the now-structural
% comonoid removed. The cartesian PROPs are therefore precisely the Lawvere
% theories of \cref{chapter:algebras}: the comonoid that was extra
% structure in a general PROP becomes the free copy and delete of
% $\catname{FinSet}^{\mathrm{op}}$, and the syntactic PROP
% $\mathbf{S}_{\Sigma,\mathcal{E}}$ collapses to the free-algebra collection
% $T_{\Sigma'}/\bar R$ viewed as a Lawvere theory.
% \end{example}

% The fact that in the above example the PROP carries a cartesian monoidal
% structure follows from a result of Fox~\cite{foxCoalgebrasCartesianCategories1976},
% which identifies the cartesian categories among the symmetric monoidal
% ones as exactly those in which every object is, naturally, a commutative
% comonoid.

% \begin{definition}[$X$-coalgebra]
% Let $X$ be a symmetric monoidal category. An \emph{$X$-coalgebra} is a
% triple $(C, \delta, \varepsilon)$ with $C \in \obj{X}$, a
% comultiplication $\delta : C \to C \otimes C$, and a counit $\varepsilon
% : C \to \I$, forming a cocommutative comonoid: $\delta$ is coassociative,
% counital, and cocommutative (i.e.\ $\delta \fatsemi \sym_{C,C} =
% \delta$). A \emph{morphism of $X$-coalgebras} $(C, \delta_C, \varepsilon_C)
% \to (D, \delta_D, \varepsilon_D)$ is a map $f : C \to D$ commuting with
% the comonoid structure,
% \[
% f \fatsemi \delta_D = \delta_C \fatsemi (f \otimes f)
% \quad\text{and}\quad
% f \fatsemi \varepsilon_D = \varepsilon_C~.
% \]
% We write $(X)^{\mathrm{C}}$ for the category of $X$-coalgebras and
% coalgebra morphisms, and $U : (X)^{\mathrm{C}} \to X$ for the forgetful
% functor $(C, \delta, \varepsilon) \mapsto C$.
% \end{definition}

% \begin{lemma}
% $(X)^{\mathrm{C}}$ is cartesian. The product of $(C, \delta_C,
% \varepsilon_C)$ and $(D, \delta_D, \varepsilon_D)$ is $C \otimes D$ with
% \[
% \delta_{C \otimes D}
% = (\delta_C \otimes \delta_D) \fatsemi (\id_C \otimes \sym_{C,D} \otimes \id_D),
% \qquad
% \varepsilon_{C \otimes D} = \varepsilon_C \otimes \varepsilon_D,
% \]
% and projections $\pi_C = \id_C \otimes \varepsilon_D$ and $\pi_D =
% \varepsilon_C \otimes \id_D$; the unit object $\I$, with its trivial
% coalgebra structure, is terminal. Under this cartesian structure the
% forgetful functor $U : (X)^{\mathrm{C}} \to X$ is strict monoidal.
% \end{lemma}

% \begin{theorem}[Fox~\cite{foxCoalgebrasCartesianCategories1976}]\label{theorem:fox}
% The assignment $X \mapsto (X)^{\mathrm{C}}$ extends to a functor
% $(-)^{\mathrm{C}} : \catname{Mon} \to \catname{Cart}$ that is right
% adjoint to the inclusion $\iota : \catname{Cart} \hookrightarrow
% \catname{Mon}$,
% \[
% \iota \dashv (-)^{\mathrm{C}} : \catname{Mon} \to \catname{Cart},
% \]
% where $\catname{Mon}$ is the category of symmetric monoidal categories and
% strict monoidal functors and $\catname{Cart}$ its full subcategory of
% cartesian ones. In particular, $X$ is cartesian if and only if the
% counit $U : (X)^{\mathrm{C}} \to X$ is an isomorphism---equivalently, if
% and only if every object of $X$ carries a (necessarily unique) natural
% commutative comonoid structure.
% \end{theorem}

% \noindent
% The category $(X)^{\mathrm{C}}$ is thus the \emph{cofree} cartesian
% category on $X$---Fox's ``best approximation to $X$ by a cartesian
% category''. Applied to \cref{ex:cartesian-prop}, the naturality
% equations make every object of $\mathbf{S}_{\Sigma,\mathcal{E}}$ a
% comonoid and every morphism a coalgebra map, so the counit is an
% isomorphism and the PROP is cartesian.

\begin{remark}\label{remark:composing_props}
PROPs, together with identity-on-objects symmetric strict monoidal functors, 
form a category $\catname{PROP}$. The PROP $\mathbf{P}$ 
of~\cref{example:initial_prop} is the initial object in $\catname{PROP}$,
since every PROP $T$ comes equipped with a unique map $\mathbf{P} \to T$
including the symmetries.

There are two ways to compose PROPs $P_1$ and $P_2$. The first is 
the \emph{coproduct} $P_1 + P_2$, computed as a pushout over $\mathbf{P}$
in $\catname{SymCat}$: it freely combines morphisms from $P_1$ and $P_2$,
subject to the relations internal to each, and identifies their shared 
symmetries (the image of $\mathbf{P}$)~\cite{bonchi_string_2022-1}. The algebras of $P_1 + P_2$ 
carry both a $P_1$-algebra structure and a $P_2$-algebra structure 
with \emph{no} compatibility conditions between them.

The second is the \emph{tensor composite} $P_1 \otimes_{\mathbf{P}} P_2$~\cite{lackDedicatedAurelioCarboni}.
Since every PROP $P$ is a $\mathbf{P}$-$\mathbf{P}$-bimodule---with 
$\mathbf{P}$ acting by pre- and post-composition with symmetries---one can form the tensor product over $\mathbf{P}$, which identifies 
the right $\mathbf{P}$-action on $P_1$ with the left $\mathbf{P}$-action 
on $P_2$. For $P_1 \otimes_{\mathbf{P}} P_2$ to itself be a PROP, one 
requires a \emph{distributive law} $\lambda \colon P_2 \otimes_{\mathbf{P}} 
P_1 \to P_1 \otimes_{\mathbf{P}} P_2$ of $\mathbf{P}$-$\mathbf{P}$-bimodules,
which provides the necessary monad multiplication and encodes how 
morphisms from $P_1$ and $P_2$ interact. The algebras of the composite 
then carry both a $P_1$-algebra structure and a $P_2$-algebra structure,
satisfying compatibility conditions determined by $\lambda$. This is how 
the bialgebra PROP arises as the tensor composite of the PROP for 
monoids and the PROP for comonoids.
\end{remark}

\begin{proposition}\label{prop:prop-ump}
There is a free--forgetful adjunction
\[
F : \catname{MonSig} \rightleftarrows \catname{PROP} : U,
\qquad F \dashv U,
\]
where $F(\Sigma) = \mathbf{S}_\Sigma$ is the free PROP on the signature
$\Sigma = (\Sigma_0, \Sigma_1)$, and $U$ sends a PROP $\mathbb{A}$ to the
signature whose sorts are the generating objects of $\mathbb{A}$ and whose
generators of type $w \to w'$ are the morphisms $\mathbb{A}(w, w')$. In
the single-sorted case $\Sigma_0 \cong \{*\}$, objects are natural numbers
and a generator of type $m \to n$ is a morphism in $\mathbb{A}(m,n)$. Thus
a PROP morphism $\mathbf{S}_\Sigma \to \mathbb{A}$ is an interpretation of
each generator of $\Sigma$ as a morphism of $\mathbb{A}$ of the
corresponding type.
\end{proposition}

% \begin{remark}
% One can draw the parallel further by defining a \emph{monoidal
% $\Sigma$-algebra}, or \emph{model}: a symmetric monoidal category
% $\mathcal C$ together with an interpretation $\alpha$ assigning
% \begin{itemize}
%   \item to each sort $s \in \Sigma_0$, an object $\alpha(s) \in \mathcal C$
%     (extended to words by $\alpha(s_1\cdots s_k) = \alpha(s_1) \otimes
%     \cdots \otimes \alpha(s_k)$), and
%   \item to each generator $f : w \to w'$ in $\Sigma_1$, a morphism
%     $\alpha(f) : \alpha(w) \to \alpha(w')$ in $\mathcal C$,
% \end{itemize}
% equivalently a symmetric monoidal functor $\mathbf{S}_\Sigma \to \mathcal
% C$. In the single-sorted case this is a triple $(\mathcal C, X, \alpha)$
% with a single carrier object $X = \alpha(*)$ and $\alpha(f) : X^{\otimes
% m} \to X^{\otimes n}$ for $f : m \to n$. The constructions of
% \cref{chapter:algebras} then carry over; we do not study models here,
% focusing on the syntactic PROPs $\mathbf{S}_\Sigma$ and
% $\mathbf{S}_{\Sigma,\mathcal{E}}$.
% \end{remark}

Finally, one can define rewriting on PROPs.
\begin{definition}[{\cite[Def. 2.17]{bonchi_string_2022-1}}]\label{def:rewriting_prop}
 A rewriting system $\mathcal{R}$ on a PROP $\mathbb{A}$ consists of a set of \emph{rewrite rules}, i.e., pairs $\langle l, r \rangle$ of morphisms $l,r : i \to j$ in $\mathbb{A}$ with the same domain and codomain.
 Given $a,b : m \to n \in \mathbb{A}(m,n)$, we say that $a$ rewrites to $b$ via $\mathcal{R}$, written $a \rewriteArrow{\mathcal{R}} b$, if they are decomposable as follows for some rule $\langle l,r \rangle \in \mathcal{R}$.
 \[
\tikzfig{./figures/string/decomposition} 
 \] 
\end{definition}

Intuitively, the occurrence of $l$ can be propagated to the side by pre- and post-composing with symmetries, which get absorbed into $a_1$ and $a_2$.
We could have equivalently defined rewriting by propagating the occurrence to the other side.

\begin{example}\label{example:bialgebra_rewriting}
Let $\Sigma$ be $\Sigma_{\mathbb{B}} \cup \{r : 2 \to 2\}$. Consider the theory $\mathbf{S}_{\Sigma, \mathcal{E}_{\mathbb{B}}}$ of bialgebras and let
\[
\mathcal{R} = \{ \adjustbox{scale=0.6}{\tikzfig{./figures/string/bialgebra_rewrite_rule}} \}, a = \adjustbox{scale=0.6}{\tikzfig{./figures/string/bialgebra_a}}, b = \adjustbox{scale=0.6}{\tikzfig{./figures/string/bialgebra_b}}.
\]
We have that $a \rewriteArrow{\mathcal{R}} b$ by the following decomposition
\[
\adjustbox{scale=0.8}{\tikzfig{./figures/string/bialgebra_decomposition}}~.
\]
The equations are justified by SMC equations together with bialgebra equations of $\mathcal{E}_{\mathbb{B}}$.
\end{example}
Given an SMT $(\Sigma, \mathcal{E})$ we can turn $\mathcal{E}$ into a rewriting system by
\[
\mathcal{R}_{\mathcal{E}} = \{\langle l, r \rangle \mid (l,r) \in \mathcal{E}\} \cup \{\langle r, l \rangle \mid (l,r) \in \mathcal{E}\}
\]

\begin{proposition}[Proposition 2.18~\cite{bonchi_string_2022-1}]
Let $c,d$ be diagrams in $\mathbf{S}_{\Sigma}$.
Then $c = d$ in $\mathbf{S}_{\Sigma, \mathcal{E}}$ if and only if $c \rewriteArrow{\mathcal{R}_{\mathcal{E}}} d$, where $\rewriteArrow{\mathcal{R}_{\mathcal{E}}}$ is a reflexive transitive closure of $\rewriteArrow{\mathcal{R}_{\mathcal{E}}}$; note that the relation is symmetric by construction.
\end{proposition}

The above proposition allows one to recharacterise $\mathbf{S}_{\Sigma, \mathcal{E}}$ as $\mathbf{S}_{\Sigma}/\mathcal{R}_{\mathcal{E}}$, the quotient of $\mathbf{S}_{\Sigma}$ by the rewriting system $\mathcal{R}_{\mathcal{E}}$.
Just as in the case of algebraic theories, we can build a corresponding reflector and compute its image using e-graphs.
As we saw in~\cref{chapter:introduction} this is inadequate as rewriting happens modulo SMC laws.
That is, this rewriting system is easy to apply using pen and paper, but mechanising it using terms is difficult: after all, these string diagrams are just visual notation for terms.
For example, in~\cref{example:bialgebra_rewriting} it was easy to eye-ball the redex for the rewrite rule, but in the actual terms we would need to apply the bifunctoriality equation to reveal the redex.
One of the main contributions of Bonchi et al.~\cite{bonchi_string_2022-1, bonchi_string_2022-2} was to provide a combinatorial representation of string diagrams as \emph{hypergraphs} with equal string diagrams being represented by isomorphic hypergraphs---recall that equality of string diagrams is defined as term equality modulo SMC equations.
In fact, this uses the insight of Joyal and Street, namely that in the symmetric case, connectivity is the only information that matters and that is encoded in the hypergraph representation.
Practically, this means that one can easily test the equality of terms in $\mathbf{S}_{\Sigma}$ by checking the corresponding hypergraph for isomorphism and can perform rewriting modulo SMC equations essentially for free: a hypegraph a priori contains all redexes that otherwise could be hidden behind the application of SMC equations in term representation.
We will review this approach next.

\section{String diagrams as hypergraphs}
Hypergraphs are a generalisation of graphs where edges can connect more than two vertices.
They have found applications in many domains ranging from database theory~\cite{faginDegreesAcyclicityHypergraphs1983} to machine learning~\cite{fengHypergraphNeuralNetworks2019}.
In this section we review the hypergraph machinery of Bonchi et al.~\cite{bonchi_string_2022-1, bonchi_string_2022-2} and show how it can be used to represent string diagrams.
Unlike other applications of hypergraphs, this particular treatment is inherently categorical as we ultimately want the hypergraphs to compose.
Throughout this section we will assume that $\Sigma_0$ component of a monoidal signature $\Sigma$ is a singleton for simplicity.
It is straightforward to adapt the definition to the multi-sorted case.
\begin{definition}[Hypergraph]\label{def:hypergraph}
  A (labelled) hypergraph $\mathcal{G}$ over a monoidal signature $\Sigma$ is a tuple
  \[
  (V,E,s,t,l),
  \]
  where $V$ is a finite set of \emph{vertices}, $E$ is a finite set of \emph{hyperedges}, $s : E \to V^{*}$ is a \emph{source} function, and $t : E \to V^{*}$ is a \emph{target} function and $l : E \to \Sigma_1$ is a \emph{labelling function} that assigns a generator to each hyperedge.
  The labelling function must respect the typing of the generator: for all edges $e$, $|s(e)| = m$ and $|t(e)| = n$, where $l(e) : m \to n$.
  We call a hypergraph \emph{discrete} if $E = \varnothing$.
  A hypergraph homomorphism $\phi : \mathcal{F} \to \mathcal{G}$ is a pair of functions $\phi_{V} : V_{\mathcal{F}} \to V_{\mathcal{G}}$ and $\phi_{E} : E_{\mathcal{F}} \to E_{\mathcal{G}}$ such that the following hold for all $e \in E_{\mathcal{F}}$.
  \begin{enumerate}
    \item $\phi_{V}^{*}(s_{\mathcal{F}}(e)) = s_{\mathcal{G}}(\phi_{E}(e))$;
    \item $\phi_{V}^{*}(t_{\mathcal{F}}(e)) = t_{\mathcal{G}}(\phi_{E}(e))$;
    \item $l_{\mathcal{F}}(e) = l_{\mathcal{G}}(\phi_{E}(e))$.
  \end{enumerate}
  We denote by $\catname{Hyp}(\Sigma)$ the category of hypergraphs over a signature $\Sigma$ and hypergraph homomorphisms.
\end{definition}

\begin{remark}
When $\Sigma_0$ component of the signature is non-trivial, we get hypergraphs with coloured or typed vertices, that is, such hypergraphs additionally have a labelling function $l_{V} : V \to \Sigma_0$ on vertices.
\end{remark}

\begin{figure}
\centering
\tikzfig{./figures/string/hypergraph_example_1}
\caption{Hypergraph example.}
\label{fig:hypergraph_example_1}
\end{figure}

\begin{example}
An example of a (labelled) hypergraph over a signature $\Sigma$ with $\Sigma_1 = \{f : 3 \to 2\}$ is given in~\cref{fig:hypergraph_example_1}.
We depict hyperedges as round boxes with the label from the signature and connect vertices to hyperedges with wires.
When there is no ambiguity, we will refer to hyperedges by their labels.
In this example, we have $s(f) = [v_1, v_2, v_3]$ and $t(f) = [w_1, w_2]$.
\end{example}

There is an equivalent definition of a hypergraph that makes the categorical properties of the category $\catname{Hyp}(\Sigma)$ more evident.
\begin{definition}
We denote by $\mathbb{F}$ the category with objects the finite sets $[n] = \{0, \ldots, n-1\}$ and morphisms the functions between the finite sets.
The tensor product is given by the disjoint union of finite sets, with the empty set as the unit.
\end{definition}
\begin{definition}\label{def:hypergraph_functor}
Let $\catname{I}$ be the category with objects the pairs of natural numbers $(k,l) \in \mathbb{N} \times \mathbb{N}$ together with an object $\star$, and with exactly $k + l$ arrows from each $(k,l)$ to $\star$, denoted $s_0, \dots, s_{k-1}$ and $t_0, \dots, t_{l-1}$.
A hypergraph over a monoidal signature $\Sigma$ is a functor $F : \catname{I} \to \mathbb{F}$ such that for each $(k,l)$, $F(k,l)$ is the set of hyperedges with $k$ sources and $l$ targets, $F(\star)$ is the set of vertices, and the arrows $s_i$ and $t_j$ are mapped to the maps assigning the $i$-th source and $j$-th target to each edge.
The category of unlabelled hypergraphs $\catname{Hyp}$ is then the functor category $\mathbb{F}^{\catname{I}}$.
Since limits and colimits in a functor category are computed pointwise and $\mathbb{F}$ has all finite limits and colimits, $\catname{Hyp}$ has all finite limits and colimits.

A monoidal signature $\Sigma$ can then be seen as a hypergraph with a single vertex and an edge for each generator.
For example, given a signature $\Sigma$ with $\Sigma_1 = \{g : 2 \to 1, f : 3 \to 2\}$, we can construct the following hypergraph
\[
\tikzfig{./figures/string/signature_hypergraph}~,
\]
where $f$ and $g$ are hyperedges (not labels).
Given a signature $\Sigma$, we denote such a hypergraph by $\mathcal{G}_{\Sigma}$.
A labelled hypergraph is then an object of the slice category $\catname{Hyp} \downarrow \mathcal{G}_{\Sigma}$, that is, a category with objects the pairs $(\mathcal{H}, l)$ where $\mathcal{H} \in \catname{Hyp}$ and $l : \mathcal{H} \to \mathcal{G}_{\Sigma}$ is a hypergraph homomorphism that maps each hyperedge of $\mathcal{H}$ to a hyperedge of $\mathcal{G}_{\Sigma}$---the label.
\end{definition}
This recovers the category $\catname{Hyp}(\Sigma)$ of the previous definition.
It also makes every labelled hypergraph well-typed by construction, as a homomorphism ensures that the labelling is compatible with the number of sources and targets.
Slicing over $\mathcal{G}_{\Sigma}$ also ensures that the category of labelled hypergraphs has all finite limits and colimits.
In particular, the two colimits that we are interested in are the coproduct and the pushout.

\begin{definition}[Coproduct]
Let $\mathcal{G}_1$ and $\mathcal{G}_2$ be hypergraphs, viewed as functors $\mathcal{G}_i : \catname{I} \to \mathbb{F}$.
Their coproduct $\mathcal{G}_1 \coprod \mathcal{G}_2 : \catname{I} \to \mathbb{F}$ is computed pointwise: it sends each object $x$ of $\catname{I}$ to the coproduct $\mathcal{G}_1(x) \coprod \mathcal{G}_2(x)$ in $\mathbb{F}$, and each arrow $\sigma : (k,l) \to \star$ to the coproduct
\[
\mathcal{G}_1(\sigma) \coprod \mathcal{G}_2(\sigma) : \mathcal{G}_1(k,l) \coprod \mathcal{G}_2(k,l) \longrightarrow \mathcal{G}_1(\star) \coprod \mathcal{G}_2(\star)
\]
of the two functions.
For labelled hypergraphs $(\mathcal{G}_1, l_1)$ and $(\mathcal{G}_2, l_2)$, with $l_i : \mathcal{G}_i \to \mathcal{G}_{\Sigma}$, the coproduct in the slice category $\catname{Hyp} \downarrow \mathcal{G}_{\Sigma}$ is $(\mathcal{G}_1 \coprod \mathcal{G}_2, [\,l_1, l_2\,])$, where $[\,l_1, l_2\,] : \mathcal{G}_1 \coprod \mathcal{G}_2 \to \mathcal{G}_{\Sigma}$ is the copairing.
This is well-defined because the forgetful functor $\catname{Hyp} \downarrow \mathcal{G}_{\Sigma} \to \catname{Hyp}$ creates colimits: the coproduct in the slice is the coproduct of the underlying hypergraphs equipped with the induced map to $\mathcal{G}_{\Sigma}$.

Equivalently, in the tuple notation of~\cref{def:hypergraph}, write $\iota^V_i : V_{\mathcal{G}_i} \hookrightarrow V_{\mathcal{G}_1} \coprod V_{\mathcal{G}_2}$ for the vertex injections and $(\iota^V_i)^{*}$ for their action on lists.
The coproduct is then
\[
\bigl(V_{\mathcal{G}_1} \coprod V_{\mathcal{G}_2},\; E_{\mathcal{G}_1} \coprod E_{\mathcal{G}_2},\; s,\; t,\; l\bigr),
\]
where the source map $s = [\,(\iota^V_1)^{*} \circ s_{\mathcal{G}_1},\; (\iota^V_2)^{*} \circ s_{\mathcal{G}_2}\,]$ relabels each source list along the vertex injection, the target map $t$ is defined analogously, and the label map $l = [\,l_{\mathcal{G}_1},\, l_{\mathcal{G}_2}\,] : E_{\mathcal{G}_1} \coprod E_{\mathcal{G}_2} \to \Sigma_1$ is the copairing, both labellings targeting the same $\Sigma_1$.
\end{definition}

\begin{definition}[Pushout]\label{def:pushout_hyp}
Let $\mathcal{G}_0$, $\mathcal{G}_1$, and $\mathcal{G}_2$ be hypergraphs, viewed as functors $\mathcal{G}_i : \catname{I} \to \mathbb{F}$.
Let $f : \mathcal{G}_0 \to \mathcal{G}_1$ and $g : \mathcal{G}_0 \to \mathcal{G}_2$ be hypergraph homomorphisms, i.e., natural transformations.
Their pushout $\mathcal{G}_1 \coprod_{\mathcal{G}_0} \mathcal{G}_2$ is computed pointwise in $\mathbb{F}^{\catname{I}}$: for each object $x$ of $\catname{I}$ it is the pushout
\[
(\mathcal{G}_1 \coprod_{\mathcal{G}_0} \mathcal{G}_2)(x) = \mathcal{G}_1(x) \coprod_{\mathcal{G}_0(x)} \mathcal{G}_2(x)
\]
in $\mathbb{F}$ of the span $\mathcal{G}_1(x) \xleftarrow{f_x} \mathcal{G}_0(x) \xrightarrow{g_x} \mathcal{G}_2(x)$, which is given by $\mathcal{G}_1(x) \coprod \mathcal{G}_2(x)$ quotiented by the least equivalence relation $\sim_x$ such that $\iota^x_1(z_1) \sim_x \iota^x_2(z_2)$ if there exists $w \in \mathcal{G}_0(x)$ with $z_1 = f_x(w)$ and $z_2 = g_x(w)$, where $\iota^x_1, \iota^x_2$ are the injections into $\mathcal{G}_1(x) \coprod \mathcal{G}_2(x)$; we denote this quotient $(\mathcal{G}_1(x) \coprod \mathcal{G}_2(x)) / {\sim_x}$.
Then, given an arrow $\sigma : (k,l) \to \star$, we get an arrow $\alpha \defeq (\mathcal{G}_1 \coprod \mathcal{G}_2)(\sigma)$, $\alpha : (\mathcal{G}_1 \coprod \mathcal{G}_2)((k,l)) \to (\mathcal{G}_1 \coprod \mathcal{G}_2)(\star)$ and then the arrow $\hat{\alpha} : (\mathcal{G}_1 \coprod \mathcal{G}_2)((k,l)) \to (\mathcal{G}_1 \coprod \mathcal{G}_2)(\star) / \sim_{\star}$ by post-composing with the quotient projection.
We then let $\tilde{\alpha}([w]) = \hat{\alpha}(w)$ for $[w] \in (\mathcal{G}_1 \coprod \mathcal{G}_2)((k,l)) / {\sim_{(k,l)}}$.
That is, the action of $\mathcal{G}_1 \coprod_{\mathcal{G}_0} \mathcal{G}_2$ on arrows is given by $\tilde{\alpha}$.
Suppose $w_1 \sim_{(k,l)} w_2$, then $\hat{\alpha}(w_1) = \hat{\alpha}(w_2)$, that is, $\tilde{\alpha}$ is well-defined.
To illustrate that, assume there exists $z \in \mathcal{G}_0((k,l))$ such that $w_1 = \iota^{(k,l)}_1 \circ f_{(k,l)}(z)$ and $w_2 = \iota^{(k,l)}_2 \circ g_{(k,l)}(z)$, then we need to check if
\begin{gather*}
\hat{\alpha}(\iota^{(k,l)}_1 \circ f_{(k,l)}(z)) = \hat{\alpha}(\iota^{(k,l)}_2 \circ g_{(k,l)}(z))\\
\iff\\
\alpha(\iota^{(k,l)}_1 \circ f_{(k,l)}(z)) \sim_{\star} \alpha(\iota^{(k,l)}_2 \circ g_{(k,l)}(z))\\
\iff\\
(\mathcal{G}_1 \coprod \mathcal{G}_2)(\sigma)(\iota^{(k,l)}_1 \circ f_{(k,l)}(z)) \sim_{\star} (\mathcal{G}_1 \coprod \mathcal{G}_2)(\sigma)(\iota^{(k,l)}_2 \circ g_{(k,l)}(z))\\
\iff\\
(\mathcal{G}_1(\sigma) \coprod \mathcal{G}_2(\sigma))(\iota^{(k,l)}_1 \circ f_{(k,l)}(z)) \sim_{\star} (\mathcal{G}_1(\sigma) \coprod \mathcal{G}_2(\sigma))(\iota^{(k,l)}_2 \circ g_{(k,l)}(z))\\
\iff\\
\iota^{\star}_1(\mathcal{G}_1(\sigma)(f_{(k,l)}(z))) \sim_{\star} \iota^{\star}_2(\mathcal{G}_2(\sigma)(g_{(k,l)}(z)))
\end{gather*}
By naturality of $f$ and $g$, we have that $\mathcal{G}_1(\sigma) \circ f_{(k,l)} = f_{\star} \circ \mathcal{G}_0(\sigma)$ and $\mathcal{G}_2(\sigma) \circ g_{(k,l)} = g_{\star} \circ \mathcal{G}_0(\sigma)$, so we have
\begin{gather*}
\iota^{\star}_1(\mathcal{G}_1(\sigma)(f_{(k,l)}(z))) \sim_{\star} \iota^{\star}_2(\mathcal{G}_2(\sigma)(g_{(k,l)}(z)))\\
\iff\\
\iota^{\star}_1(f_{\star}(\mathcal{G}_0(\sigma)(z))) \sim_{\star} \iota^{\star}_2(g_{\star}(\mathcal{G}_0(\sigma)(z))).
\end{gather*}
The last line holds by definition of $\sim_{\star}$.
The rest follows by a more detailed case analysis on $w_1 \sim_{(k,l)} w_2$.
Checking that $\tilde{\alpha}$ preserves identities and composition, so that $\mathcal{G}_1 \coprod_{\mathcal{G}_0} \mathcal{G}_2$ is indeed a functor, is routine.
Now, given a span $(\mathcal{G}_1, l_1) \xleftarrow{f} (\mathcal{G}_0, l_0) \xrightarrow{g} (\mathcal{G}_2, l_2)$ in $\catname{Hyp} \downarrow \mathcal{G}_{\Sigma}$, the pushout is $(\mathcal{G}_1 \coprod_{\mathcal{G}_0} \mathcal{G}_2, l)$, where $l : \mathcal{G}_1 \coprod_{\mathcal{G}_0} \mathcal{G}_2 \to \mathcal{G}_{\Sigma}$ is the unique map given by the universal property of the pushout.

This can be adapted to the case of labelled hypergraphs given by tuples in the same vein as the coproduct.
\end{definition}
We also recall some vocabulary of hypergraphs.

\begin{definition}[Degree]
A \emph{degree} of a vertex is the number of hyperedges that are incident to it, i.e., the number of hyperedges $e$ such that $v$ is in $s(e)$ or $t(e)$;
\emph{in-degree} and \emph{out-degree} are the number of hyperedges $e$ such that $v$ is in $t(e)$ and $s(e)$ respectively.
\end{definition}

\begin{definition}[Path]
A \emph{path} in a hypergraph is a sequence of vertices and hyperedges $[v_0, e_1, v_1, \ldots, e_n, v_n]$ such that $v_i \in s(e_{i+1})$ and $v_{i+1} \in t(e_{i+1})$.
A hypergraph is \emph{acyclic} if it has no path that contains the same vertex twice.
\end{definition}

Just like in the case of topological string diagrams where we had the ordering on the boundary (either induced by slicing or by choice), to make hypergraphs denote string diagrams we need a notion of a boundary equipped with an ordering.
This is achieved by considering a category of cospans of hypergraphs.

\begin{definition}[Category $\catname{HypI}(\Sigma)$]\label{def:hypI}
Let $D : \mathbb{F} \to \catname{Hyp}(\Sigma)$ be the coproduct-preserving functor that maps each $[n]$ to the discrete hypergraph with vertex set $[n]$, and each function $h : [m] \to [n]$ to the homomorphism of discrete hypergraphs acting as $h$ on vertices.
We let $\catname{HypI}(\Sigma)$ be the category with objects $D([n])$ for $[n] \in \mathbb{F}$ and morphisms isomorphism classes of cospans in $\catname{Hyp}(\Sigma)$ of the form
\[
D([m]) \xrightarrow{f_{\mathrm{in}}} \mathcal{G} \xleftarrow{g_{\mathrm{out}}} D([n]);
\]
two cospans $D([m]) \xrightarrow{f_{\mathrm{in}}} \mathcal{G} \xleftarrow{g_{\mathrm{out}}} D([n])$ and $D([m]) \xrightarrow{f'_{\mathrm{in}}} \mathcal{H} \xleftarrow{g'_{\mathrm{out}}} D([n])$ are isomorphic if $\mathcal{G} \cong \mathcal{H}$ and the following diagram commutes:
\[\begin{tikzcd}
	&& {\mathcal{G}} && \\
	{D([m])} &&&& {D([n])} \\
	&& {\mathcal{H}}
	\arrow["\cong"{description}, tail reversed, from=1-3, to=3-3]
	\arrow["{f_{\mathrm{in}}}", from=2-1, to=1-3]
	\arrow["{f'_{\mathrm{in}}}"', from=2-1, to=3-3]
	\arrow["{g_{\mathrm{out}}}"', from=2-5, to=1-3]
	\arrow["{g'_{\mathrm{out}}}", from=2-5, to=3-3]
\end{tikzcd}~.
\]
The composition of cospans $D([m]) \to \mathcal{G} \leftarrow D([n])$ and $D([n]) \to \mathcal{H} \leftarrow D([k])$ is given by the pushout of $\mathcal{G}$ and $\mathcal{H}$ over $D([n])$ in $\catname{Hyp}(\Sigma)$:
\[
\adjustbox{scale=0.7}{
\begin{tikzcd}
	{D([m])} &&&& {D([n])} &&&& {D([k])} \\
	\\
	&& {\mathcal{G}} &&&& {\mathcal{H}} \\
	\\
	&&&& {\mathcal{G} \mathop{\coprod}\limits_{D([n])}\mathcal{H}}
	\arrow["{f_{\mathrm{in}}}", from=1-1, to=3-3]
	\arrow["{g_{\mathrm{out}}}"', from=1-5, to=3-3]
	\arrow["{f'_{\mathrm{in}}}", from=1-5, to=3-7]
	\arrow["{g'_{\mathrm{out}}}"', from=1-9, to=3-7]
	\arrow[from=3-3, to=5-5]
	\arrow[from=3-7, to=5-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=135}, draw=none, from=5-5, to=1-5]
\end{tikzcd}}
\]
Since pushout is defined up to isomorphism, we take isomorphism classes of cospans as morphisms.
The identity morphism is given by the cospan
\[
D([n]) \xrightarrow{\id} D([n]) \xleftarrow{\id} D([n]).
\]
The monoidal product $\otimes$ on objects is given by the coproduct $D([n]) \otimes D([m]) \defeq D([n]) \sqcup D([m])$, and its action on morphisms by the coproduct in $\catname{Hyp}(\Sigma)$ as follows:
\begin{align*}
    D([n]) \xrightarrow{f} &\mathcal{G} \xleftarrow{g} D([m])\\
     &\otimes \qquad\qquad\qquad \defeq D([n]) \sqcup D([k]) \xrightarrow{f \sqcup f'} \mathcal{G} \sqcup \mathcal{H} \xleftarrow{g \sqcup g'} D([m]) \sqcup D([l])\\ 
    D([k]) \xrightarrow{f'} &\mathcal{H} \xleftarrow{g'} D([l])~,
\end{align*}
with the unit $\I = D([0])$ the empty hypergraph, and $\id_{\I}$ the cospan of empty hypergraphs
\[
D([0]) \xrightarrow{\id} D([0]) \xleftarrow{\id} D([0]).
\]
The symmetry is given by the cospan 
\[\begin{tikzcd}
	{D([n]\sqcup[m])} && {D([m]\sqcup [n])} \\
	{D([n])\sqcup D([m])} && {D([m])\sqcup D([n])} \\
	& {D([n])\sqcup D([m])}
	\arrow["\cong"{description}, draw=none, from=1-1, to=2-1]
	\arrow["\cong"{description}, draw=none, from=1-3, to=2-3]
	\arrow["\id", from=2-1, to=3-2]
	\arrow["{D(\sym_{[m],[n]})}"', from=2-3, to=3-2]
\end{tikzcd}\]
that is, the symmetry is induced by the universal property of the coproduct in $\mathbb{F}$.

The above makes $\catname{HypI}(\Sigma)$ a PROP.
\end{definition}

\begin{remark}
The functor $D : \mathbb{F} \to \catname{Hyp}(\Sigma)$ equips the vertices of each discrete hypergraph $D([n])$ with a linear order: a vertex $v$ precedes a vertex $w$ whenever $v$ is the image of a smaller element in $[n] = \{0, \dots, n-1\}$.
Consequently, the legs of a cospan pick out an \emph{ordered} boundary, which is what is needed to represent the ordered inputs and outputs of a string diagram.
Henceforth, we omit the functor $D$ and write cospans simply as $m \xrightarrow{} \mathcal{G} \xleftarrow{} n$.
The name $\catname{HypI}(\Sigma)$ is meant to suggest that hypergraphs are \emph{I}nterfaced by the legs of the cospan.
The left leg denotes the \emph{input} interface, and the right leg denotes the \emph{output} interface.
\end{remark}

\begin{remark}
In the coloured case, the functor $D$ is considered as a functor $D : (\mathbb{F} \downarrow \Sigma_0) \longrightarrow \catname{Hyp}(\Sigma)$, with the domain being a slice category with objects the pairs $([n], f)$ with $f$ labelling each element of $[n]$ with a colour from $\Sigma_0$.
The morphisms are then colour-preserving functions between the finite sets.
Equivalently, this category has objects words of $\Sigma_0$ with monoidal structure given by concatenation.
\end{remark}

We will next use the terms cospans and morphisms interchangeably when talking about morphisms in $\catname{HypI}(\Sigma)$.

\begin{example}[Example 3.7~\cite{bonchi_string_2022-1}]\label{example:hypergraph_category_structure}
  Consider the following morphisms $G : 2 \to 3$ and $H : 3 \to 1$ in $\catname{HypI}(\Sigma)$
  \[
  G \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/G_example}} \qquad H \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/H_example}}~.
  \]
  We draw cospans oriented as string diagrams: with the input at the bottom and the output at the top.
  We use labels to define the maps corresponding to input and output interfaces of the cospan and use subscripts to induce the ordering on the boundary.
  Using the definition, we can form $G \fatsemi H : 2 \to 1$ and $G \otimes H : 5 \to 4$ as follows:
  \[
  G \fatsemi H \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/GH_composed_example}} \qquad G \otimes H \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/GH_tensored_example}}~.
  \]
  The former is computed by taking the pushout along the common interface $D({1,2,3})$.
  When we compute the pushout along a discrete hypergraph, it morally amounts to identifying vertices that share the same pre-image in this hypergraph by \enquote{glueing} them together.

  Below, we list the cospans for the identity and symmetry morphisms in $\catname{HypI}(\Sigma)$.
  \[
  \id_1 \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/id_example}} \qquad \id_{\I} \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/id_I_example}} \qquad \sym_{1,1} \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/sym_example}}~.
  \]

\end{example}

We now have all the ingredients to interpret string diagrams, i.e., morphisms in $\mathbf{S}_{\Sigma}$, as morphisms in $\catname{HypI}(\Sigma)$.
By freeness of $\mathbf{S}_{\Sigma}$, it suffices to define the interpretation on generators of $\Sigma$ and extend it to all morphisms by composition and monoidal product.
\begin{definition}
Given a monoidal signature $\Sigma = (\{*\}, \Sigma_1)$, we define the interpretation function $\llbracket - \rrbracket : \Sigma_1 \to U(\catname{HypI}(\Sigma))$ as follows:
\[
\llbracket f : m \to n \rrbracket \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/f_interpretation}}~.
\]
By~\cref{prop:prop-ump}, it induces a unique strong symmetric monoidal functor $\llbracket - \rrbracket : \mathbf{S}_{\Sigma} \to \catname{HypI}(\Sigma)$.
\end{definition}

So far, we have shown that every string diagram can be interpreted as a hypergraph, but it turns out that the definition of a $\catname{HypI}(\Sigma)$ is too general for every cospan to be interpreted as a string diagram.
Consider, for example, the following morphism in $\catname{HypI}(\Sigma)$:
\[
\adjustbox{scale=0.8}{\tikzfig{./figures/string/SMC_non_example}}~.
\]
There is no string diagram generators such that composing them would result in a loop, also, recalling the string diagrams of Joyal and Street, the polarised diagrams did not have cycles.
Another reason why the cospan fails to be a string diagram is that the vertices $v_1$ and $v_2$ from the input interface are mapped to the same vertex in the carrier, intuitively, such a map would correspond to a monoid structure on the generating object which is not available in general.
In fact, morphisms in $\catname{HypI}(\Sigma)$ correspond to string diagrams for a free \emph{Frobenius} PROP.

\begin{definition}\label{def:frobenius_prop}
Consider an SMT $(\Sigma_{\text{Frob}}, \mathcal{E}_{\text{Frob}})$ with $\Sigma_{\text{Frob}}$ given by
\[
\Sigma_{\text{Frob}} \defeq \{\adjustbox{scale=0.3}{\tikzfig{./figures/string/monoid_generator}} : 2 \to 1, \adjustbox{scale=0.6}{\tikzfig{./figures/string/unit_generator}} : 0 \to 1, \adjustbox{scale=0.3}{\tikzfig{./figures/string/comonoid_generator}} : 1 \to 2, \adjustbox{scale=0.6}{\tikzfig{./figures/string/counit_generator}} : 1 \to 0 \};
\]
and $\mathcal{E}_{\text{Frob}}$ given by the equations
\begin{gather}
\adjustbox{width=0.8\textwidth}{\tikzfig{./figures/string/monoid_equations}}\label{eq:monoid_frob}\\[2pt]
\;
\adjustbox{width=0.8\textwidth}{\tikzfig{./figures/string/comonoid_equations}}\label{eq:comonoid_frob}\\[2pt]
\;
\adjustbox{width=0.5\textwidth}{\tikzfig{./figures/string/frobenius_equations}}\label{eq:frobenius_frob}
\end{gather}
\cref{eq:monoid_frob} and \cref{eq:comonoid_frob} are the commutative monoid and commutative comonoid equations, respectively, for monoid and comonoid generators, just the ones we had in the case of bialgebra in~\cref{fig:bialgebra_generators_equations}---generators depicted with dots with the reason becoming clear later---and \cref{eq:frobenius_frob} is the Frobenius equation stipulating the interaction between the monoid and comonoid structures---again like in the case of bialgebras, just with a different interaction rule.
We let $\mathbf{Frob}$ denote the free PROP presented by the SMT $(\Sigma_{\text{Frob}}, \mathcal{E}_{\text{Frob}})$.
We then let $\mathbf{H}_{\Sigma, \mathcal{E}}$ be the PROP obtained by taking a coproduct $\mathbf{S}_{\Sigma,\mathcal{E}} \coprod \mathbf{Frob}$ in $\catname{PROP}$---see~\cref{remark:composing_props} for details.
It can be equivalently presented by an SMT $(\Sigma \cup \Sigma_{\text{Frob}}, \mathcal{E} \cup \mathcal{E}_{\text{Frob}})$---note that $\Sigma_{\text{Frob}}$ is not part of $\Sigma$ which is the parameter of $\mathbf{H}_{\Sigma, \mathcal{E}}$ and similarly for $\mathcal{E}_{\text{Frob}}$.
\end{definition}

\begin{remark}
  More precisely, the above is the presentation of a theory of commutative Frobenius algebras and in particular only one interaction rule between monoid and comonoid is given, the dual rule is derivable using symmetry:
  \[
  \adjustbox{scale=0.7}{\tikzfig{./figures/string/frobenius_interaction_dual}}~.
  \]
  The non-commutative version features two interaction rules explicitly.
\end{remark}

\begin{definition}\label{def:frobenius_interpretation}
Given a monoidal signature $\Sigma_{\text{Frob}} = (\{*\}, \Sigma_1)$, we define the interpretation function $[-] : \Sigma_1 \to U(\catname{HypI}(\Sigma_{\text{Frob}}))$ as follows:
\begin{gather*}
 [\adjustbox{scale=0.3}{\tikzfig{./figures/string/monoid_generator}}] \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/monoid_interpretation}}~,
 [\;\adjustbox{scale=0.6}{\tikzfig{./figures/string/unit_generator}}] \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/unit_interpretation}}~,
 [\adjustbox{scale=0.3}{\tikzfig{./figures/string/comonoid_generator}}] \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/comonoid_interpretation}}~,
 [\;\adjustbox{scale=0.6}{\tikzfig{./figures/string/counit_generator}} ] \defeq \adjustbox{scale=0.8}{\tikzfig{./figures/string/counit_interpretation}}~.
\end{gather*}
This induces a unique strong symmetric monoidal functor $[-] : \mathbf{Frob} \to \catname{HypI}(\Sigma_{\text{Frob}})$.
We then let $\llangle - \rrangle : \mathbf{H}_{\Sigma} \to \catname{HypI}(\Sigma)$ be the copairing $[\llbracket - \rrbracket, [-]]$.
\end{definition}

\begin{theorem}[Theorem 4.1~\cite{bonchi_string_2022-1}]\label{thm:frobenius_isomorphism}\label{theorem:frobenius_isomorphism}
  The functor $\llangle - \rrangle : \mathbf{H}_{\Sigma} \to \catname{HypI}(\Sigma)$ is an isomorphism of PROPs, i.e., $\mathbf{H}_{\Sigma} \cong \catname{HypI}(\Sigma)$.
\end{theorem}
\begin{proof}
We present a sketch of the proof here, and refer the reader to~\cite{bonchi_string_2022-1} for the full details.
The idea is to show that $\catname{HypI}(\Sigma)$ has the universal property of the coproduct, namely that for any PROP $\mathbb{A}$ and PROP morphisms $\alpha : \mathbf{S}_{\Sigma} \to \mathbb{A}$ and $\beta : \catname{Frob} \to \mathbb{A}$, there exists a unique PROP morphism $\gamma$ such that the following diagram commutes:
\[\begin{tikzcd}
	{\mathbf{S}_{\Sigma}} && {\catname{HypI}(\Sigma)} && {\catname{Frob}} \\
	\\
	&& {\mathbb{A}}
	\arrow["{\llbracket - \rrbracket}", from=1-1, to=1-3]
	\arrow["\alpha"', from=1-1, to=3-3]
	\arrow["\gamma", from=1-3, to=3-3]
	\arrow["{[-]}"', from=1-5, to=1-3]
	\arrow["\beta"', from=1-5, to=3-3]
\end{tikzcd}~.\]
As all the arrows are identity-on-objects functors, it suffices to show that every morphism in $\catname{HypI}(\Sigma)$ can be decomposed in a unique way into components such that every component is in the image of either $\llbracket - \rrbracket$ or $[-]$.
Let $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ be a morphism in $\catname{HypI}(\Sigma)$ and let $\mathcal{G} = (V_{\mathcal{G}}, E_{\mathcal{G}}, s, t, l)$ be its carrier hypergraph.
Let $e_1, \ldots, e_n$ be some ordering on $E_{\mathcal{G}}$.
Let $\tilde{m} \xrightarrow{\tilde{f}} \tilde{E} \xleftarrow{\tilde{g}} \tilde{n}$ be a cospan with the carrier formed by edges from $\mathcal{G}$, disconnected from each other and stacked vertically with $\tilde{m}$ being the collection of all input vertices for each edge and $\tilde{m}$ being the collection of all output vertices for each edge.
There are functions $i : n \to V_{\mathcal{G}}$,  $\tilde{i} : \tilde{n} \to V_{\mathcal{G}}$, $j : m \to V_{\mathcal{G}}$, and $\tilde{j} : \tilde{m} \to V_{\mathcal{G}}$ that map vertices in the interfaces to their occurrences in the set of all vertices.
The decomposition of $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ is then given by the composition of cospans
% \begin{gather}\label{eq:decomposition}
% \displaystyle (m \xrightarrow{i} V_{\mathcal{G}} \xleftarrow{[\id, \tilde{j}]} V_{\mathcal{G}} \otimes \tilde{m}) \fatsemi (V_{\mathcal{G}} \otimes \tilde{m} \xrightarrow{\id \otimes \tilde{f}} V_{\mathcal{G}} \otimes \tilde{E} \xleftarrow{\id \otimes \tilde{g}} V_{\mathcal{G}} \otimes \tilde{n}) \fatsemi (V_{\mathcal{G}} \otimes \tilde{n} \xrightarrow{[\id, \tilde{i}]} V_{\mathcal{G}} \xleftarrow{i} n)~.
% \end{gather}
\begin{equation}\label{eq:decomposition}
  \begin{adjustbox}{scale=0.9}
\begin{tikzcd}
	m & {V_{\mathcal{G}}} & {V_{\mathcal{G}}\otimes\tilde{m}} & {V_{\mathcal{G}}\otimes\tilde{E}} & {V_{\mathcal{G}}\otimes\tilde{n}} & {V_{\mathcal{G}}} & n
	\arrow["i", from=1-1, to=1-2]
	\arrow["{{[\id,\tilde{j}]}}"', from=1-3, to=1-2]
	\arrow["{{\id\otimes\tilde{f}}}", from=1-3, to=1-4]
	\arrow["{{\id\otimes\tilde{g}}}"', from=1-5, to=1-4]
	\arrow["{{[\id,\tilde{i}]}}", from=1-5, to=1-6]
	\arrow[from=1-6, to=1-5]
	\arrow["i"', from=1-7, to=1-6]
\end{tikzcd}
\end{adjustbox}
\end{equation}
For example, below is a cospan (on the left) and its decomposition (on the right); we showed the embedding of the interfaces using dashed arrows to avoid excessive cluttering of the cospans and used the labels to assign identities to vertices in $V_{\mathcal{G}}$.
\[
\adjustbox{scale=0.8}{\tikzfig{./figures/string/piling_example_1}} \quad \cong \adjustbox{scale=0.8}{\tikzfig{./figures/string/piling_example_3}} \fatsemi\; \adjustbox{scale=0.8}{\tikzfig{./figures/string/piling_example_2}} \fatsemi \adjustbox{scale=0.8}{\tikzfig{./figures/string/piling_example_4}}~.
\]
By computing the pushouts for the compositions on the right, we can verify that the composition is indeed isomorphic to the cospan on the left. 
Intuitively, the middle cospan records all edges and vertices of the carrier hypergraph and the cospans on the sides record the incidence of the hyperedges and vertices.
For example, in the first composition, vertices $u_1, u_3$ of hyperedges $a$ and $b$ get identified with the vertex $z_1$ according to the interface embedding.

The middle cospan of the decomposition~\ref{eq:decomposition} is in the image of $\llbracket - \rrbracket$ as it is a tensor of identities and generators from $\Sigma$.
The cospans on the sides are essentially cospans of finite sets and hence in the image of $[-]$ as $\catname{Frob} \cong \catname{Cospan}(\mathbb{F})$---a result of Lack~\cite{lackDedicatedAurelioCarboni}.
This means that every morphism $\gamma : \catname{HypI}(\Sigma) \to \mathbb{A}$ is uniquely determined by its action on the components of the decomposition of each morphism.
\end{proof}

\begin{proposition}[Corollary 4.3~\cite{bonchi_string_2022-1}]
\label{prop:faithful_interpretation}
  The functor $\llbracket - \rrbracket : \mathbf{S}_{\Sigma} \to \catname{HypI}(\Sigma)$ is faithful.
\end{proposition}

\begin{remark}
The same result holds when $\mathbf{S}_{\Sigma}$ is a coloured PROP.
\end{remark}

PROPs equipped with a Frobenius algebra structure on each object constitute a subcategory of $\catname{PROP}$, called $\catname{FROP}$, with $\catname{H}_{\Sigma}$ and $\catname{H}_{\Sigma, \mathcal{E}}$ being the instances of FROPs.
The notion of syntactic rewriting for FROPs is the same with the only difference that rewriting happens modulo SMC equations and the equations of Frobenius algebra.
\begin{definition}[{\cite[p. 15]{bonchi_string_2022-1}}]\label{def:rewriting_frop}
 A rewriting system $\mathcal{R}$ on a FROP $\mathbb{A}$ consists of a set of \emph{rewrite rules}, i.e., pairs $\langle l, r \rangle$ of morphisms $l,r : i \to j$ in $\mathbb{A}$ with the same domain and codomain.
 Given $a,b : m \to n \in \mathbb{A}(m,n)$, we say that $a$ rewrites to $b$ via $\mathcal{R}$, written $a \rewriteArrow{\mathcal{R}} b$, if they are decomposable as follows for some rule $\langle l,r \rangle \in \mathcal{R}$.
 \[
\tikzfig{./figures/string/decomposition} 
 \] 
\end{definition}

In order to get a full and faithful characterisation of morphisms in $\mathbf{S}_{\Sigma}$, we need to restrict the morphisms in $\catname{HypI}(\Sigma)$; such a characterisation will automatically be an isomorphism as the categories have the same objects, i.e., the characterisation is essentially surjective by construction.
\begin{definition}[Monogamous cospans (Def. 13~\cite{bonchi_string_2022-2})]
  \label{def:monogamous_cospans}
  A cospan $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ in $\catname{HypI}(\Sigma)$ is \emph{monogamous} if $f$ and $g$ are mono and for all vertices $v$ of $\mathcal{G}$,
  \[
  \text{in-degree of } v = \begin{cases}
  0 & \text{if } v \in f(m)\\
  1 & \text{otherwise}
  \end{cases}
  \]
  \[
  \text{out-degree of } v = \begin{cases}
  0 & \text{if } v \in g(m)\\
  1 & \text{otherwise}
  \end{cases}
  \]
\end{definition}
Practically, a cospan is monogamous if no vertex is incident to more than one hyperedge, the vertices in the image of the input (output) interface have no incoming (outgoing) hyperedges, and the interface maps $f$ and $g$ are monic.
\begin{example}
All the cospans in~\cref{def:frobenius_interpretation} fail to be monogamous: the input interface of the first cospan is not monic, the output interface of the third cospan is not monic, the out-degree of the single vertex in the carrier of the cospan number two must be one for the cospan to be monogamous as the vertex is not in the image of the output interface, similarly for the single vertex in the carrier of the last cospan: its in-degree is zero but it is not in the image of the input interface.
The following cospan is not monogamous
\[
\adjustbox{scale=0.8}{\tikzfig{./figures/string/non_monogamous_example}}~,
\]
because vertices $v_1$ and $w_1$ have in- and out-degree of $2$ respectively.
In contrast, all the cospans in~\cref{example:hypergraph_category_structure} are monogamous.
\end{example}

\begin{definition}[Acyclic cospans (Definition 20~\cite{bonchi_string_2022-2})]
  A cospan $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ in $\catname{HypI}(\Sigma)$ is \emph{acyclic} if the carrier hypergraph $\mathcal{G}$ is acyclic.
\end{definition}

\begin{definition}
  A cospan $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ in $\catname{HypI}(\Sigma)$ is \emph{monogamous acyclic} if it is both monogamous and acyclic.
  We will call such cospans mda-cospans for short.
\end{definition}

\begin{proposition}
The objects of $\;\catname{HypI}(\Sigma)$ together with the mda-cospans form a full subcategory of $\;\catname{HypI}(\Sigma)$ which we denote by $\catname{MHypI}(\Sigma)$.
\end{proposition}
\begin{proof}
The cospans for identities and symmetries are monogamous acyclic.
The composition and tensoring of two mda-cospans are also mda-cospans.
\end{proof}

Before we can state that the characterisation of morphisms of $\mathbf{S}_{\Sigma}$ in $\catname{MHypI}(\Sigma)$ is fully faithful, we will need a technical lemma and a definition.

\begin{definition}[Def. 22~\cite{bonchi_string_2022-2}]
  A sub-hypergraph $\mathcal{H}$ of a hypergraph $\mathcal{G}$ is \emph{convex} if for all vertices $v_1, v_2$ in $\mathcal{H}$, every hyperedge on a path between $v_1$ and $v_2$ in $\mathcal{G}$ is also in $\mathcal{H}$.
\end{definition}

\begin{example}[Ex. 23~\cite{bonchi_string_2022-2}]
Below, for the hypergraph on the left, there are two sub-hypergraphs (outlined) with the first one being a convex sub-hypergraph as all hyperedges on the one and only path between vertices $v_1$ and $v_2$ are in the sub-hypergraph, and the second one failing to be a convex sub-hypergraph because a path between vertices $v_2$ and $v_3$ contains a hyperedge not in the sub-hypergraph.
\[
\adjustbox{scale=0.8}{\tikzfig{./figures/string/convex_hypergraph_example_1}} \qquad \adjustbox{scale=0.8}{\tikzfig{./figures/string/convex_hypergraph_example_2}} \qquad \adjustbox{scale=0.8}{\tikzfig{./figures/string/convex_hypergraph_example_3}}~.
\]
\end{example}

\begin{lemma}[Lem. 4~\cite{bonchi_string_2022-2}]
\label{lemma:convex_decomposition}
Let $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ be an mda-cospan in $\catname{MHypI}(\Sigma)$ and let $\mathcal{L}$ be a convex sub-hypergraph of $\mathcal{G}$.
Then there exists $k \in \mathbb{N}$ and an mda-cospan $i \xrightarrow{} L \xleftarrow{} j$ such that $\mathcal{G}$ factors as
\begin{equation}\label{eq:convex_decomposition}
\tikzfig{./figures/string/convex_decomposition_1}~,
\end{equation}
where all cospans are mda-cospans.
\end{lemma}
\begin{proof}
Let $\mathcal{C}_{1}$ be the smallest hypergraph that contains the input vertices of $\mathcal{G}$ and contains all hyperedges $e$ that are not in $\mathcal{L}$ but such that there is a path from $e$ to a vertex in $\mathcal{L}$.
Let $\mathcal{C}_{2}$ be the smallest hypergraph that contains the outputs of $\mathcal{G}$ such that $\mathcal{G} = \mathcal{C}_{1} \cup \mathcal{L} \cup \mathcal{C}_{2}$.
By minimality, the three hypergraphs $\mathcal{C}_{1}$, $\mathcal{L}$, and $\mathcal{C}_{2}$ overlap only on vertices.
Let
\begin{gather*}
i \defeq V_{\mathcal{C}_{1}} \cap V_{\mathcal{L}}\\
j \defeq V_{\mathcal{C}_{2}} \cap V_{\mathcal{L}}\\
k \defeq (V_{\mathcal{C}_{1}} \cap V_{\mathcal{C}_{2}}) \setminus V_{\mathcal{L}}~.
\end{gather*}
Now define the following cospans where all arrows are mono
\begin{gather*}
m \xrightarrow{} \mathcal{C}_{1} \xleftarrow{} k \coprod i\\
i \xrightarrow{} \mathcal{L} \xleftarrow{} j\\
k \coprod j \xrightarrow{} \mathcal{C}_{2} \xleftarrow{} n~.
\end{gather*}
Then \ref{eq:convex_decomposition} is computed by taking pushouts for the diagram below
\[
m \xrightarrow{} \mathcal{C}_{1} \xleftarrow{} k \coprod i \xrightarrow{} k \coprod \mathcal{L} \xleftarrow{} k \coprod j \xrightarrow{} \mathcal{C}_{2} \xleftarrow{} n~.
\]
The two spans identify those vertices from $\mathcal{G}$ that occur in more than one sub-hypergraph, so this amounts to simply taking the union
\[
m \xrightarrow{} \mathcal{C}_{1} \cup \mathcal{L} \cup \mathcal{C}_{2} \xrightarrow{} n = m \xrightarrow{} \mathcal{G} \xleftarrow{} n~.
\]
$\mathcal{C}_{1}$, $\mathcal{L}$, and $\mathcal{C}_{2}$ are all acyclic as $\mathcal{G}$ is acyclic, so it remains to check that all components are monogamous.
By $\mathcal{G}$ being monogamous, the in- and out- degrees of vertices in $\mathcal{G}$ are at most $1$, so checking that the cospans are monogamous amounts to checking whether the vertices with in-degree (out-degree) $0$ are exactly in the image of the input (output) interface.
$\mathcal{C}_{1}$ is closed under predecessors and hence vertices with in-degree $0$ are exactly in the image of $m$.
Suppose there exists a vertex $v$ in $\mathcal{C}_{1}$ with out-degree $0$ that is not in the image of $k \coprod i$, then there must be a hyperedge $e$ such that $v$ is its source; $e$ can not be in either $\mathcal{L}$ or $\mathcal{C}_{2}$ as otherwise $v$ would be in the image of $k \coprod i$, but then we get a contradiction since $\mathcal{G} = \mathcal{C}_{1} \cup \mathcal{L} \cup \mathcal{C}_{2}$.
$\mathcal{C}_{2}$ is monogamous by noting that it is closed under successors and by mirroring the argument for $\mathcal{C}_{1}$.
Now, given a vertex $v$ in $\mathcal{L}$ with no input hyperedge, it must be in the image of $i$.
Suppose it is not, then there must be a hyperedge $e$ such that $v$ is its target as otherwise $v$ would be in the input interface of $\mathcal{G}$ and hence in the image of $i$.
As $e$ is not in $\mathcal{L}$, it must be in $\mathcal{C}_{1}$ as there is a path from $e$ to $v$ which is in $\mathcal{L}$ and therefore $v$ must be in $\mathcal{C}_{1}$ and hence must be in $i$ by construction, which is a contradiction.
Now, given a vertex $v$ in $\mathcal{L}$ with no output hyperedge, it must be in the image of $j$.
Suppose it is not, then there must be a hyperedge $e$ such that $v$ is its source.
We will show that $e$ must be in $\mathcal{C}_{2}$; suppose it is in $\mathcal{C}_{1}$, then by construction of $\mathcal{C}_{1}$ there is a path containing $e$ and $v$ to $\mathcal{L}$, since $\mathcal{L}$ is convex, $e$ must be in $\mathcal{L}$ which is a contradiction and therefore $e$ is in $\mathcal{C}_{2}$ as well as $v$.
Then, by construction, $v$ must be in $j$.
\end{proof}
\begin{theorem}[Theorem 25~\cite{bonchi_string_2022-2} and Corollary 26~\cite{bonchi_string_2022-2}]\label{theorem:monogamous_isomorphism}
$\mathbf{S}_{\Sigma} \cong \catname{MHypI}(\Sigma)$.
\end{theorem}
\begin{proof}
Recall the functor $\llbracket - \rrbracket : \mathbf{S}_{\Sigma} \to \catname{HypI}(\Sigma)$.
As the interpretation $\llbracket - \rrbracket : \Sigma_1 \to U(\catname{HypI}(\Sigma))$ produces mda-cospans and mda-cospans are closed under composition and tensoring and since symmetries and identities are mda-cospans, we have a restriction $\llbracket - \rrbracket : \mathbf{S}_{\Sigma} \to \catname{MHypI}(\Sigma)$. 
By \cref{prop:faithful_interpretation}, the functor $\llbracket - \rrbracket : \mathbf{S}_{\Sigma} \to \catname{MHypI}(\Sigma)$ is faithful and hence its restriction is also faithful.
It remains to show that the functor is full.
Let $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ be an mda-cospan in $\catname{MHypI}(\Sigma)$.
We will show that there exists a morphism $F : m \to n$ in $\mathbf{S}_{\Sigma}$ such that $\llbracket F \rrbracket = m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$.
We will do this by induction on the number of hyperedges in $\mathcal{G}$.
\paragraph{Base case:} If $\mathcal{G}$ does not contain any hyperedges, then it is a discrete hypergraph and the monogamy condition implies that $f$ and $g$ are bijections and therefore the cospan is the image of the morphism that is a mix of identities and symmetries.
\paragraph{Inductive step:} Pick any hyperedge $e$ in $\mathcal{G}$. Recall that $l(e) : \Sigma_1$ is a morphism in $\mathbf{S}_{\Sigma}$.
Furthermore, a single hyperedge is a convex sub-hypergraph of $\mathcal{G}$ and hence by the lemma above, we can decompose the cospan as in~\cref{eq:convex_decomposition} with $\mathcal{L} = \llbracket l(e) \rrbracket$.
By the induction hypothesis, there exist morphisms $F_1 : m \to k \coprod i$, $F_2 : k \coprod j \to n$ and $l(e) : i \to j$ for each component of the decomposition and by functoriality of $\llbracket - \rrbracket$, $m \xrightarrow{} \mathcal{G} \xleftarrow{} n$ has a pre-image in $\mathbf{S}_{\Sigma}$ and the functor is full. 
As $\llbracket - \rrbracket$ is fully faithful and is identity-on-objects, it is an isomorphism of PROPs.
\end{proof}

\begin{remark}
Again, the same result holds for the multi-sorted version of $\mathbf{S}_{\Sigma}$.
\end{remark}

The major payoff of~\cref{theorem:frobenius_isomorphism} and~\cref{theorem:monogamous_isomorphism} is that we can now reason about string diagrams (equivalence classes of terms in $\mathbf{H}_{\Sigma}$ and $\mathbf{S}_{\Sigma}$, respectively) using the corresponding notion of cospans of hypergraphs.
First, it makes it straightforward to check whether two string diagrams are equal by checking whether the corresponding cospans are isomorphic which can be checked by picking a \enquote{canonical} cospans by choosing a particular traversal for the carrier out of the equivalence class of cospans and therefore this characterisation may be used to efficiently represent string diagrams in a computer---this is the insight we will later use to generalise e-graphs so that they handle symmetric monoidal theories more efficiently.
Second, it allows us to reason about string diagrams using graph rewriting: noting that the cospans of $\catname{HypI}(\Sigma)$ and $\catname{MHypI}(\Sigma)$ contain all the redexes modulo SMC and Frobenius equations and modulo SMC equations, respectively, on the nose.
By performing graph rewriting on the cospans and by taking the appropriate quotients, we can obtain $\mathbf{H}_{\Sigma, \mathcal{E}}$ and $\mathbf{S}_{\Sigma, \mathcal{E}}$.
We will recall the (categorical) notion of graph rewriting in the next section and then show how it applies in the cases of $\catname{HypI}(\Sigma)$ and $\catname{MHypI}(\Sigma)$.

\section{DPO(I) Rewriting}\label{sec:dpo_rewriting}

Double-pushout (DPO) rewriting, introduced by Ehrig et al.~\cite{ehrigGraphgrammarsAlgebraicApproach1973} is the classical categorical approach to graph transformation, generalising term rewriting from trees to graph-like structures. 
Where a term-rewriting rule $l \to r$ replaces a subterm matching $l$ with $r$ in some bigger term $t = C[l]$ producing a term $t' = C[r]$, where $C$ is a context----a term-with-a-hole together with a notion of substitution, with $r$; a DPO rule is a span $L \xleftarrow{} K \xrightarrow{} R$ in some category $\mathcal{C}$:
$L$ is the pattern to match, $R$ is its replacement, and $K$ is a shared "interface" — the part of $L$ that must be preserved (not deleted) and glued unchanged into $R$. A rewriting step consists of two pushout squares:
\begin{equation}\label{eq:dpo_rewriting}
  \begin{tikzcd}
	L && K && R \\
	\\
	G && {L^{\bot}} && H
	\arrow[from=1-1, to=3-1]
	\arrow[from=1-3, to=1-1]
	\arrow[from=1-3, to=1-5]
	\arrow[from=1-3, to=3-3]
	\arrow[from=1-5, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=90}, draw=none, from=3-1, to=1-3]
	\arrow[from=3-3, to=3-1]
	\arrow[from=3-3, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=180}, draw=none, from=3-5, to=1-3]
\end{tikzcd}
\end{equation}
where the left square identifies a match of $L$ inside the source object $G$ together with its complement $L^{\bot}$---a \emph{pushout complement}, generalised from the notion of a context $C[-]$---and the right square glues $R$  back onto $L^{\bot}$ to produce the result $H$. 
Intuitively, the two pushouts amount to \emph{deleting} the occurrence of those parts of $L$ that have no pre-image in $K$ in $G$ and \emph{gluing} $R$ into the hole.
The name \enquote{double pushout} refers to these two squares.

One of the main benefits of this approach is that it can be studied regardless of the particular category as long as it has pushouts which allows one to translate the properties of abstract rewriting between the categories.
By further requiring that pushouts in a given category are \emph{well-behaved}, such categories are referred to as \emph{adhesive}, it can be shown that DPO rewriting has some useful properties that apply to a wide range of categories.
Our interest is primarily in treating DPO rewriting as a tool to perform rewriting efficiently on string diagrams.

DPO rewriting is not the only categorification of graph rewriting.
Other approaches include single-pushout (SPO) rewriting~\cite{HandbookGraphGrammars2003}, which features a single pushout in a category of partial morphisms with the nature of a pushout in such a category responsible for deleting and gluing at the same time.
The deletion in SPO rewriting has more leeway than in DPO rewriting, which does not interact well with the rewriting modulo SMC equations since the latter impose strict restrictions on what may and may not be deleted to constitute a redex.
A different tradition altogether is that of higher-dimensional rewriting in the sense of Burroni~\cite{burroniHigherdimensionalWordProblems1993}, later developed by Guiraud and Mimram~\cite{guiraudTerminationOrdersThreedimensional2006,mimram3DimensionalRewritingTheory2014}, in which a rewriting system on the morphisms of an $n$-category is presented by an $(n+1)$-polygraph: the terms being rewritten are $n$-cells and the rewriting rules themselves are $(n+1)$-cells, applied in context by whiskering with identity cells.
String diagram rewriting is then an instance of $2$-polygraphs.
This framework is considerably more general than what we will describe in this section, as it presupposes no monoidal structure whatsoever; the price of this generality is that the laws of symmetric monoidal categories are not built in and must themselves be adjoined as explicit (invertible) rewriting cells, so that any analysis of the resulting system---termination or confluence in particular---must account for these structural rules alongside the rules of the theory of interest.
The hypergraph representation avoids this overhead entirely, since the SMC equations hold on the nose in $\catname{HypI}(\Sigma)$.
Closest to the present approach are the open graphs (also called string graphs) of Dixon and Kissinger~\cite{dixonOpengraphsMonoidalTheories2013}, which likewise encode morphisms of a symmetric monoidal category as graphs with boundary and reason equationally via DPO rewriting, with adhesivity obtained by embedding open graphs into the adhesive category of typed graphs rather than exhibited directly.
The crucial difference is soundness: unrestricted DPO rewriting on open graphs is sound only in the presence of a traced symmetric monoidal structure, as a match may wrap around its context in a way that requires a feedback loop to be expressed syntactically, whereas the convexity and boundary conditions of~\cite{bonchi_string_2022-2} rule out precisely such matches and thereby guarantee soundness for arbitrary SMCs.

\begin{example}[Example 3.15~\cite{bonchi_string_2022-1}]\label{example:dpo}
  Consider the following span (on the left) in $\catname{HypI}(\Sigma)$ and the hypergraph in which it has a matching (on the right):
  \[
  \adjustbox{scale=0.7}{
  \tikzfig{./figures/string/dpo_example_1} \qquad\qquad \tikzfig{./figures/string/dpo_example_2}~.
  }
  \]
  This yields the following DPO square
  \[
  \adjustbox{scale=0.7}{
  \tikzfig{./figures/string/dpo_example_3}~.
  }
  \]
  The pushout complement is computed by deleting the hyperedges and vertices in $L$ with no pre-image in $K$, i.e. keeping those which are the image of $v_1$ and $w_1$.
  The span intuitively corresponds to one of the two rewrite rules induced by $\mathcal{E}$: $\langle \adjustbox{scale=0.65}{\tikzfig{./figures/string/dpo_example_4}}, \adjustbox{scale=0.65}{\tikzfig{./figures/string/dpo_example_5}} \rangle$ for a corresponding presentation.
\end{example}

While pushouts are characterised by their universal property, pushout complements are not and may not exist, or there may be many of them.
There is a well-known result that for a category of directed graphs a pushout complement exists when so-called \emph{no-dangling} and \emph{no-identification} conditions are satisfied~\cite{corradiniAlgebraicApproachesGraph1997}.
This result is usually cited verbatim for hypergraphs as well even though these are different objects.
It is straightforward to adapt the notions to the case of hypergraphs and show that the same result holds.

\begin{definition}[No-dangling condition (Definition 3.16~\cite{bonchi_string_2022-1})]\label{def:no_dangling}
  Morphisms $K \xrightarrow{i} L \xrightarrow{f} G$ satisfy \emph{no-dangling} condition if for every hyperedge $e$ not in the image of $f$, its sources and targets are either not in the image of $f$ or in the image of $f \circ i$.
\end{definition}
Intuitively, this condition ensures that when we should not delete vertices that are incident to hyperedges which are not being deleted.
\begin{example}\label{example:no-dangling}
The left square in~\cref{example:dpo} satisfies the no-dangling condition as the only hyperedge not in the image of $f$ is the hyperedge labelled $f$ and its source vertex is in the image of $f \circ i$ and the target vertex is not in the image of $f$ at all.
Below is the example of morphisms that do not satisfy the condition due to Kaye~\cite[Example 9.14]{kayeFoundationsDigitalCircuits2025}.
\[
\tikzfig{./figures/string/no_dangling_example}~.
\]
In particular, the vertices $v_3$ and $v_4$ incident to the hyperedge $e_2$ are in the image of $f$.
An intuitive reason for why the no-dangling condition is necessary is that a pushout complement is computed---when the morphism $i$ is mono---by deleting the hyperedges and vertices in the image of $f$ keeping everything in the image of $f \circ i$ intact.
An attempt to perform such a deletion would result in the following picture
\[
\tikzfig{./figures/string/no_dangling_example_2}~.
\]
The alleged pushout complement is not a valid hypergraph as a hyperedge labelled $e_2$ must have a source and a target; a homomorphism between hypergraphs must also preserve the number of inputs and outputs, so there is simply no map from the alleged pushout complement to the source hypergraph. 
\end{example}
The other vital condition is the no-identification condition.
\begin{definition}[No-identification condition~{\cite[Def. 3.17]{bonchi_string_2022-1}}]\label{def:no_identification}
  Morphisms $K \xrightarrow{i} L \xrightarrow{f} G$ satisfy the \emph{no-identification} condition if any two distinct items of $L$ (vertices or hyperedges) identified by $f$ lie in the image of $i$.
\end{definition}
\begin{example}
Any monic $f$ trivially satisfies the condition, which is the case both in~\cref{example:dpo} and~\cref{example:no-dangling}.
An example of morphisms that do not satisfy the condition, again due to Kaye~\cite[Example 9.16]{kayeFoundationsDigitalCircuits2025}, is given below.
\[
\tikzfig{./figures/string/no_identification_example}~.
\]
Computing the alleged pushout complement by deleting the hyperedges and vertices in the image of $f$ and keeping everything in the image of $f \circ i$ gives us the following square.
\[
\tikzfig{./figures/string/no_identification_example_2}~.
\]
Again, computing the pushout does not give us back the source hypergraph because there is no way to identify the vertices $v_3$ and $v_4$ using just the hypergraph $K$.
\end{example}

\begin{proposition}\label{prop:pushout_complement_exists}
  Let $K \xrightarrow{i} L \xrightarrow{f} G$ be morphisms in $\catname{Hyp}(\Sigma)$.
  A pushout complement of $K \xrightarrow{i} L \xrightarrow{f} G$ exists if and only if the no-dangling and no-identification conditions are satisfied.
\end{proposition}
\begin{proof}
  For the if direction, suppose the two conditions hold, and define $L^{\bot}$ as the following sub-hypergraph of $G$
  \[
  L^{\bot} \defeq (V_{L^{\bot}}, E_{L^{\bot}}, s_{L^{\bot}}, t_{L^{\bot}}, l_{L^{\bot}}) \hookrightarrow G, 
  \]
  where 
  \begin{gather*}
    V_{L^{\bot}} \defeq \bigl(V_G \setminus f(V_L)\bigr) \cup f(i(V_K))\\
    E_{L^{\bot}} \defeq \bigl(E_G \setminus f(E_L)\bigr) \cup f(i(E_K))\\
    s_{L^{\bot}} \defeq s_G \restriction_{E_{L^{\bot}}} \quad t_{L^{\bot}} \defeq t_G \restriction_{E_{L^{\bot}}} \quad l_{L^{\bot}} \defeq l_G \restriction_{E_{L^{\bot}}}
  \end{gather*}
  This is a well-defined sub-hypergraph: a hyperedge of $L^{\bot}$ either lies outside the image of $f$, in which case the no-dangling condition places each of its sources and targets outside the image of $f$ or inside the image of $f \circ i$, hence in $V_{L^{\bot}}$; or it lies in the image of $f \circ i$, in which case its sources and targets lie in the image of $f \circ i$ as well, since $f \circ i$ is a homomorphism.
  Let $g \colon L^{\bot} \hookrightarrow G$ be the inclusion and $c \colon K \to L^{\bot}$ the corestriction of $f \circ i$, which lands in $L^{\bot}$ by construction; then $g \circ c = f \circ i$, so the square commutes.

  It remains to show the square is a pushout.
  Let $P \defeq L \coprod_{K} L^{\bot}$ be the pushout of $L \xleftarrow{i} K \xrightarrow{c} L^{\bot}$ computed as in~\cref{def:pushout_hyp}: the vertices and edges of the resulting hypergraph are quotients $V_{L} \coprod_{V_{K}} V_{L^{\bot}} / \sim$ and $E_{L}\coprod_{E_{K}} E_{L^{\bot}} / \sim$ by the least equivalence relation identifying $\iota_{1}^{V}(i_{V}(z))$ with $\iota_{2}^{V}(c_{V}(z))$ for $z \in V_{K}$ and similarly for edges.
  Let $u \colon P \to G$ be the homomorphism induced by $f$ and $g$ via the universal property, given explicitly on vertices by $u_{V}([\iota^{V}_{1}(w)]) = f_{V}(w)$ and $u_{V}([\iota^{V}_{2}(z)]) = g_{V}(z)$, and analogously on edges.
  We show that $u_{V}$ and $u_{E}$ are bijections; since a homomorphism that is bijective on vertices and on edges is an isomorphism of hypergraphs, this suffices.

  For surjectivity, a vertex of $G$ either lies in $f(V_{L})$, hence in the image of $u_{V} \circ [\iota^{V}_{1}]$, or lies in $V_{G} \setminus f(V_{L}) \subseteq V_{L^{\bot}}$, hence in the image of $u_{V} \circ [\iota^{V}_{2}]$; the same argument applies to edges.

  For injectivity, note that $u_{V}$ is injective if and only if any two elements of $V_{L} \coprod V_{L^{\bot}}$ with the same image under the copairing $[f_{V}, g_{V}] \colon V_{L} \coprod V_{L^{\bot}} \to V_{G}$ are related by $\sim$: the copairing factors as $u_{V}$ after the quotient projection.
  So suppose two elements of $V_{L} \coprod V_{L^{\bot}}$ have the same image under $[f_{V}, g_{V}]$; we show that they are related by $\sim$.
  There are three cases.
  If both are of the form $\iota^{V}_{2}(z_{1}), \iota^{V}_{2}(z_{2})$, then $g_{V}(z_{1}) = g_{V}(z_{2})$ implies $z_{1} = z_{2}$, since $g$ is an inclusion.
  If both are of the form $\iota^{V}_{1}(w_{1}), \iota^{V}_{1}(w_{2})$ with $w_{1} \neq w_{2}$, then $f_{V}(w_{1}) = f_{V}(w_{2})$, so by the no-identification condition $w_{1} = i_{V}(z_{1})$ and $w_{2} = i_{V}(z_{2})$ for some $z_{1}, z_{2} \in V_{K}$.
  Then $c_{V}(z_{1}) = f_{V}(i_{V}(z_{1})) = f_{V}(w_{1}) = f_{V}(w_{2}) = c_{V}(z_{2})$, where the outer equalities hold because $c$ is the corestriction of $f \circ i$, and hence
  \[
  \iota^{V}_{1}(w_{1}) = \iota^{V}_{1}(i_{V}(z_{1})) \sim \iota^{V}_{2}(c_{V}(z_{1})) = \iota^{V}_{2}(c_{V}(z_{2})) \sim \iota^{V}_{1}(i_{V}(z_{2})) = \iota^{V}_{1}(w_{2}).
  \]
 Finally, suppose the elements are $\iota^{V}_{1}(w)$ and $\iota^{V}_{2}(z)$ with $f_{V}(w) = g_{V}(z)$.
  Since $g_{V}(z) \in V_{L^{\bot}}$ and $f_{V}(w) \in f(V_{L})$, the construction of $V_{L^{\bot}}$ yields $f_{V}(w) \in f(i(V_{K}))$, say $f_{V}(w) = f_{V}(i_{V}(y))$ for some $y \in V_{K}$.
  If $w = i_{V}(y)$, then
  \[
  g_{V}(c_{V}(y)) = f_{V}(i_{V}(y)) = f_{V}(w) = g_{V}(z),
  \]
  where the first equality is the commutativity $g \circ c = f \circ i$; injectivity of $g_{V}$ then gives $c_{V}(y) = z$, and hence $\iota^{V}_{1}(w) = \iota^{V}_{1}(i_{V}(y)) \sim \iota^{V}_{2}(c_{V}(y)) = \iota^{V}_{2}(z)$.
  Otherwise, $w$ and $i_{V}(y)$ are distinct vertices identified by $f$, so by the no-identification condition $w = i_{V}(y')$ for some $y' \in V_{K}$, and the previous argument applies with $y'$ in place of $y$.
  In all cases the two elements are related by $\sim$, so $u_{V}$ is injective; the argument for $u_{E}$ is identical, using the no-identification condition for edges in the second case.
  Hence $u$ is an isomorphism, the constructed square is a pushout, and $L^{\bot}$ is a pushout complement of $K \xrightarrow{i} L \xrightarrow{f} G$.

  For the converse, suppose that a pushout complement $K \xrightarrow{c} L^{\bot} \xrightarrow{g} G$ exists but at least one of the conditions fails.
  Since pushouts are unique up to an isomorphism commuting with the pushout injections, we may by~\cref{def:pushout_hyp} assume that $V_{G} = V_{L} \coprod V_{L^{\bot}} / {\sim}$ and $E_{G} = E_{L} \coprod E_{L^{\bot}} / {\sim}$, with $f$ and $g$ the composites of the coproduct injections with the quotient projections.
  We first record a consequence of this description: if $w \in V_{L}$ and $\iota^{V}_{1}(w)$ is related by $\sim$ to any element of the coproduct other than itself, then $w \in i(V_{K})$.
  Indeed, every generating pair of $\sim$ is of the form $\iota^{V}_{1}(i_{V}(z)) \sim \iota^{V}_{2}(c_{V}(z))$, so a non-trivial chain of generating pairs passing through $\iota^{V}_{1}(w)$ must contain a generator whose left component is $\iota^{V}_{1}(w)$ itself, forcing $w = i_{V}(z)$ for some $z \in V_{K}$.
  The same holds for edges.

  Suppose the no-identification condition fails: there are distinct items of $L$, say vertices $w_{1} \neq w_{2}$ (the case of edges is identical), with $f_{V}(w_{1}) = f_{V}(w_{2})$ and at least one of them outside the image of $i$.
  Then $\iota^{V}_{1}(w_{1})$ and $\iota^{V}_{1}(w_{2})$ are distinct elements of the coproduct in the same $\sim$-class, so both are related by $\sim$ to an element other than themselves, and by the consequence above both lie in the image of $i_{V}$---a contradiction.

  Suppose the no-dangling condition fails: there is a hyperedge $e \in E_{G}$ outside the image of $f$ with a source or target $v \in f(V_{L}) \setminus f(i(V_{K}))$, say $v = f_{V}(w)$ with $w \notin i(V_{K})$.
  Since $f$ and $g$ are jointly surjective on edges and $e \notin f(E_{L})$, we have $e = g_{E}(e')$ for some $e' \in E_{L^{\bot}}$.
  As $g$ is a homomorphism, $g_{V}^{*}(s_{L^{\bot}}(e')) = s_{G}(e)$ and likewise for targets, so the corresponding source or target $z$ of $e'$ satisfies $g_{V}(z) = v = f_{V}(w)$.
  Then $\iota^{V}_{1}(w)$ and $\iota^{V}_{2}(z)$ are distinct elements of the coproduct in the same $\sim$-class, so $\iota^{V}_{1}(w)$ is related by $\sim$ to an element other than itself, and by the consequence above $w \in i(V_{K})$, so we, again, reach a contradiction.
\end{proof}

Traditionally, the objects in the diagram~\ref{eq:dpo_rewriting} are (hyper-)graphs, but in the context of string diagrams we have hypergraphs with interfaces, prescribing which vertices are the inputs and outputs of the hypergraph.
To maintain the input and output interfaces of the hypergraph during DPO rewriting, the formalism has been extended to the one of DPOI rewriting, with \emph{I} standing for \emph{interfaces}, where in addition to the preservation of $K$ in the rewriting step, the interfaces of the source graph must be preserved.
\begin{definition}[DPOI rewriting~{\cite[p. 27]{bonchi_string_2022-1}}]\label{def:dpoi_rewriting}
Given a category $\mathcal{C}$ with pushouts, and given morphisms $G \xleftarrow{} J$ and $H \xleftarrow{} J$, $G$ rewrites into $H$ with interface $J$---written $G \xleftarrow{} J \rightsquigarrow_{\mathcal{R}} H \xleftarrow{} J$---if there exists a rule $L \xleftarrow{} K \xrightarrow{} R$ and an object $L^{\bot}$ such that the following diagram commutes and the two marked squares are pushouts.
\[\begin{tikzcd}
	L && K && R \\
	\\
	G && {L^{\bot}} && H \\
	&& J
	\arrow[from=1-1, to=3-1]
	\arrow[from=1-3, to=1-1]
	\arrow[from=1-3, to=1-5]
	\arrow[from=1-3, to=3-3]
	\arrow[from=1-5, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=90}, draw=none, from=3-1, to=1-3]
	\arrow[from=3-3, to=3-1]
	\arrow[from=3-3, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=180}, draw=none, from=3-5, to=1-3]
	\arrow[from=4-3, to=3-1]
	\arrow[from=4-3, to=3-3]
	\arrow[from=4-3, to=3-5]
\end{tikzcd}\]
\end{definition}
\begin{remark}\label{remark:dpoi_interface_swap}
The above formulation may cause suspicion as to why it may apply to morphisms in $\catname{HypI}(\Sigma)$ since the latter have two separate interfaces.
Given a morphism $m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ in $\catname{HypI}(\Sigma)$, we note that the cospan $0 \xrightarrow{} \mathcal{G} \xleftarrow{[f,g]} m \coprod n$ conveys the same data so in the context of DPOI rewriting when applied to hypergraphs with interfaces we pick $J$ to be the coproduct of the input and output interface and the corresponding morphism to be the copairing of the two interface morphisms, i.e., the same applies to morphisms in $\catname{MHypI}(\Sigma)$.
\end{remark}
The corresponding pushout complement must further respect the interfaces.
\begin{example}[{\cite[Ex. 3.21]{bonchi_string_2022-1}}]
Consider the same rule as in~\cref{example:dpo}
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/dpo_example_1}}~,
\]
and the same hypergraph, this time accompanied by the interface
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/dpoi_example_2}}~.
\]
The corresponding DPOI rewriting step is then given by the following diagram
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/dpoi_example_3}}~.
\]
The diagram is not much different from the one in~\cref{example:dpo} except for the additional interface $J$ and the corresponding morphisms to the source and target hypergraphs, however, the presence of the interface may affect the existence of the pushout complement.
For example, if the source was instead
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/dpoi_example_4}}~,
\]
there would be no pushout complement and therefore no match of the rule in the source hypergraph.
\end{example}
We will first recall the rewriting as it applies to $\catname{HypI}(\Sigma)$ and then will turn our attention to the case of $\catname{MHypI}(\Sigma)$.
The ultimate goal is to reconcile the rewriting of string diagrams as defined in~\cref{def:rewriting_prop} and~\cref{def:rewriting_frob}.
String diagrams in $\mathbf{H}_{\Sigma}$ have two interfaces as well as their image under $\llangle - \rrangle$.
For the cospans we managed to reduce to a single interface by considering a copairing, but it turns out that a similar \enquote{reduction} is possible in the syntactic domain.

Recall the structure of Frobenius algebra from~\cref{def:frobenius_prop}, we can abbreviate the following combinations as $\cap : 2 \to 0$ and $\cup : 0 \to 2$:
\[
\adjustbox{scale=0.7}{
\tikzfig{./figures/string/cap_definition} \qquad \tikzfig{./figures/string/cup_definition}}~.
\]
The $n$-ary versions of the above are defined by interspersing multiple $\cap$ and $\cup$ with symmetries, for example, $\cup : 0 \to n + n$ is given by
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cup_4_definition}}~.
\]
And using the equations of Frobenius algebra, we can show that the following holds
\begin{gather}
\adjustbox{scale=0.7}{
\tikzfig{./figures/string/cup_cap_interaction} \qquad \tikzfig{./figures/string/cup_sym} \qquad \tikzfig{./figures/string/cap_sym}}\label{eq:cup_cap_interaction}~.
\end{gather}
\begin{remark}
The first equality above follows from the following chain
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cup_cap_interaction_proof}}~.
\]
Where the first $=$ follows by the first equation of~\cref{eq:frobenius_frob} and the last two are unit and counit for monoid and comonoid from~\cref{eq:monoid_frob} and~\cref{eq:comonoid_frob}, respectively.
This can also be established by simply computing the composition in $\catname{HypI}(\Sigma)$---the payoff of the characterisation we discussed earlier.
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cup_cap_interaction_proof_hypergraph}}~.
\]
As the theory is commutative, we also have the equality
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cap_cup_interaction}}~.
\]
\end{remark}

These allow us to bend the inputs of string diagrams into outputs by defining the operation $\corner{-}$.
\begin{definition}
$\corner{-} : \mathbf{H}_{\Sigma}(m,n) \to \mathbf{H}_{\Sigma}(0,m + n)$ is given by  $\corner{a} = \cup_{m} \fatsemi (\id_{m} \otimes a)$, or, diagrammatically,
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/bending_wires}}~.
\]
\end{definition}
This operation can be inverted by post-composing with $\cap_{m} \otimes \id_{n}$ and applying the first equation of Equations~\ref{eq:cup_cap_interaction}.
\begin{proposition}[{\cite[Prop. 4.8]{bonchi_string_2022-1}}]
  Suppose $\llangle a \rrangle = m \xrightarrow{f} \mathcal{G} \xleftarrow{g} n$ in $\catname{HypI}(\Sigma)$, then $\llangle \corner{a} \rrangle$ produces a cospan isomorphic to $0 \xrightarrow{} \mathcal{G} \xleftarrow{[f,g]} m \coprod n$.
\end{proposition}
Using $\corner{-}$ allows us to pass freely between syntactic rewriting of $\Sigma$-terms with non-trivial inputs and rewriting of $\Sigma$-terms that have only outputs. 

\begin{theorem}[{\cite[Thm. 4.9]{bonchi_string_2022-1}}] \label{theorem:dpoi_rewriting_frob}
  Let $\langle l, r \rangle$ be a rewrite rule in $\mathbf{H}_{\Sigma}$.
  Then,
  \[
  a \rewriteArrow{\langle l, r \rangle} b \quad \iff \quad \llangle \corner{a} \rrangle \rightsquigarrow_{\langle \llangle \corner{l} \rrangle, \llangle \corner{r} \rrangle \rangle} \llangle \corner{b} \rrangle
  \]
\end{theorem}
\begin{proof}
For the only-if direction, $a \rewriteArrow{\langle l, r \rangle} b$ by definition means that there exists a decomposition of $a$ and $b$ as
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/decomposition}}~. 
\]
Applying $\corner{-}$ to the above gives us the decomposition of $\corner{a}$ as
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cup_decomposition}}~,
\]
or, by abbreviating
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cup_decomposition_2}}~,
\]
as
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cup_decomposition_3}}~.
\]
Analogously, we also get
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/cup_decomposition_4}}~.
\]
The dashed lines result in the following
\begin{equation}
\corner{a} = \corner{l} \fatsemi \tilde{a} \qquad \corner{b} = \corner{r} \fatsemi \tilde{a}\label{eq:tilde_decomposition}~,
\end{equation}
where $\tilde{a} = (\id_{i} \otimes \cup_{k} \otimes \id_{j}) \fatsemi (a_1^{\star} \otimes a_2)$.
Applying the functor $\llangle - \rrangle$ to the above gives us the following cospans
\begin{gather*}\label{gather:cospans_frobenius_rewriting}
\llangle \corner{l} \rrangle = 0 \xrightarrow{} L \xleftarrow{} i \coprod j \qquad \llangle \corner{r} \rrangle = 0 \xrightarrow{} R \xleftarrow{} i \coprod j\\
\llangle \tilde{a} \rrangle = i \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} m \coprod n\\
\llangle \corner{a} \rrangle = 0 \xrightarrow{} G \xleftarrow{} m \coprod n \qquad \llangle \corner{b} \rrangle = 0 \xrightarrow{} H \xleftarrow{} m \coprod n~.
\end{gather*}
By functoriality and by~\cref{eq:tilde_decomposition}, we have
\[
\llangle \corner{a} \rrangle = \llangle \corner{l} \rrangle \fatsemi \llangle \tilde{a} \rrangle \qquad \llangle \corner{b} \rrangle = \llangle \corner{r} \rrangle \fatsemi \llangle \tilde{a} \rrangle~,
\]
since composition of cospans is given by pushouts, we get the diagram
\[\begin{tikzcd}
	L && {i \coprod j} && R \\
	\\
	G && {L^{\bot}} && H \\
	&& {m \coprod n}
	\arrow[from=1-1, to=3-1]
	\arrow[from=1-3, to=1-1]
	\arrow[from=1-3, to=1-5]
	\arrow[from=1-3, to=3-3]
	\arrow[from=1-5, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=90}, draw=none, from=3-1, to=1-3]
	\arrow[from=3-3, to=3-1]
	\arrow[from=3-3, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=180}, draw=none, from=3-5, to=1-3]
	\arrow[from=4-3, to=3-1]
	\arrow[from=4-3, to=3-3]
	\arrow[from=4-3, to=3-5]
\end{tikzcd}\]
and by~\cref{def:dpoi_rewriting} we have $\llangle \corner{a} \rrangle \rightsquigarrow_{\langle \llangle \corner{l} \rrangle, \llangle \corner{r} \rrangle \rangle} \llangle \corner{b} \rrangle$.

For the if direction, suppose $\llangle \corner{a} \rrangle \rightsquigarrow_{\langle \llangle \corner{l} \rrangle, \llangle \corner{r} \rrangle \rangle} \llangle \corner{b} \rrangle$.
Then, by~\cref{def:dpoi_rewriting}, there exists a pushout complement $L^{\bot}$ such that the diagram as above commutes with two squares being pushouts.
We denote the cospans involved in the DPOI square as in~\cref{gather:cospans_frobenius_rewriting}.
Given $i \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} m \coprod n$ pick $\hat{a}$ in $\mathbf{H}_{\Sigma}$ such that $\llangle \hat{a} \rrangle = i \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} m \coprod n$, which exists by fullness of $\llangle - \rrangle$.
Because composition in $\catname{HypI}(\Sigma)$ is given by pushouts, we have
\begin{equation}\label{eq:only_if_direction}
\adjustbox{scale=0.85}{$\displaystyle
\begin{gathered}
  \llangle \corner{a} \rrangle = (0 \to G \leftarrow m \coprod n)
  = (0 \to L \leftarrow i \coprod j) \fatsemi (i \coprod j \to L^{\bot} \leftarrow m \coprod n)
  = \llangle \corner{l} \rrangle \fatsemi \llangle \hat{a} \rrangle = \llangle \corner{l} \fatsemi \hat{a} \rrangle\\
  \llangle \corner{b} \rrangle = (0 \to H \leftarrow m \coprod n)
  = (0 \to R \leftarrow i \coprod j) \fatsemi (i \coprod j \to L^{\bot} \leftarrow m \coprod n)
  = \llangle \corner{r} \rrangle \fatsemi \llangle \hat{a} \rrangle = \llangle \corner{r} \fatsemi \hat{a} \rrangle
\end{gathered}
$}
\end{equation}
By faithfulness of $\llangle - \rrangle$, the decompositions in~\cref{eq:only_if_direction} lift to decompositions of $\corner{a} = \corner{l} \fatsemi \hat{a}$ and $\corner{b} = \corner{r} \fatsemi \hat{a}$:
\[
\adjustbox{scale=0.8}{\tikzfig{./figures/string/cup_decomposition_5}}~.
\]
By inverting $\corner{-}$, we get the decompositions of $a$ and $b$ as
\[
\adjustbox{scale=0.8}{\tikzfig{./figures/string/cup_decomposition_6}}~,
\]
with dashed lines outlining the parts relevant to~\cref{def:rewriting_frop}, i.e. the part above the top dashed line corresponds to $a_2$ and the part below the bottom dashed line corresponds to $a_1$. 
This, by definition, means that $a \rewriteArrow{\langle l, r \rangle} b$.
\end{proof}
\begin{corollary}
\[
\mathbf{H}_{\Sigma, \mathcal{E}} \cong \catname{HypI}(\Sigma) / \mathcal{R}~,
\]
where
\[
\mathcal{R} = \{ \langle \llangle \corner{l} \rrangle, \llangle \corner{r} \rrangle \rangle \mid l = r \in \mathcal{E} \} \cup \{ \langle \llangle r \rrangle, \llangle l \rrangle \rangle \mid l = r \in \mathcal{E}\}~,
\]
and $/ \mathcal{R}$ denotes the quotient of $\catname{HypI}(\Sigma)$ by the rewriting rules $\rightsquigarrow_\mathcal{R}^{*}$ with $\rightsquigarrow^{*}$ being the reflexive transitive closure of $\rightsquigarrow$.
Namely, we say that $m_1 \xrightarrow{} \mathcal{G}_1 \xleftarrow{} n_1 \rightsquigarrow_{\mathcal{R}} m_2 \xrightarrow{} \mathcal{G}_2 \xleftarrow{} n_2$ if $0 \xrightarrow{} \mathcal{G}_1 \xleftarrow{} m_1 \coprod n_1 \rightsquigarrow 0 \xrightarrow{}_{\mathcal{R}} \mathcal{G}_2 \xleftarrow{} m_2 \coprod n_2$.
\end{corollary}
This is similar to how $\mathbf{T}_{\Sigma, \mathcal{E}}$ is the quotient of $\mathbf{T}_{\Sigma}$ by the equations $\mathcal{E}$.

Before we recall a similar result for rewriting in $\catname{MHypI}(\Sigma)$ and $\mathbf{S}_{\Sigma}$ we note that DPOI rewriting as formulated in~\cref{def:dpoi_rewriting} is too general for the case of $\catname{MHypI}(\Sigma)$ and in order to make sure that rewriting is sound and complete for rewriting in $\mathbf{S}_{\Sigma}$ the notion of pushout complement and the notion of a match must be adjusted.

\begin{example}[{\cite[Ex. 29]{bonchi_string_2022-2}}]
Consider $\Sigma_1 = \{\alpha_1 : 0 \to 1, \alpha_2 : 1 \to 0, \alpha_3 : 1 \to 1\}$ and a PROP rewriting system $\mathcal{R} = \{\;\adjustbox{scale=0.7}{\tikzfig{./figures/string/id_expression}} \Rightarrow{} \adjustbox{scale=0.7}{\tikzfig{./figures/string/alpha_3}}\}$.
It gets interpreted in $\catname{HypI}(\Sigma)$ as
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/non_mda_rewrite_example}}~.
\]
The feature of this rule is that there exist several pushout complements for it, for example, the following two are possible

\begin{minipage}{0.45\textwidth}
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/non_mda_rewrite_example_2}}
\]
\vspace{0.5em}
\end{minipage}
\hfill
\begin{minipage}{0.45\textwidth}
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/non_mda_rewrite_example_3}}
\]
\vspace{0.5em}
\end{minipage}

The target of the first DPOI square is a valid morphism in $\catname{MHypI}(\Sigma)$ and hence in $\mathbf{S}_{\Sigma}$, while the target of the second DPOI square is not---it fails to be an mda-cospan as vertex $w_1$ has in-degree $2$.
The target of the second DPOI square is instead a morphism in $\catname{HypI}(\Sigma)$ and corresponds to the following syntactic rewrite in $\mathbf{H}_{\Sigma}$, recall that rewriting in $\mathbf{H}_{\Sigma}$ is modulo SMC equations and equations of Frobenius algebra,
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/frobenius_rewrite_example}}~.
\]
\end{example}

To mitigate the above issue, we will restrict the notion of a pushout complement.
\begin{definition}[{\cite[Def. 30]{bonchi_string_2022-2}}]\label{def:boundary_pushout_complement}
For mda-cospans $i \xrightarrow{a_1} L \xleftarrow{a_2} j$ and $m \xrightarrow{b_1} G \xleftarrow{b_2} n$ and mono $f : L \to G$, a pushout complement
% https://q.uiver.app/#q=
\[\begin{tikzcd}
	L && {i \coprod j} \\
	\\
	G && {L^{\bot}} \\
	&& {m \coprod n}
	\arrow["f"', from=1-1, to=3-1]
	\arrow["{[a_1,a_2]}"', from=1-3, to=1-1]
	\arrow["{[c_1,c_2]}", from=1-3, to=3-3]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=90}, draw=none, from=3-1, to=1-3]
	\arrow[from=3-3, to=3-1]
	\arrow["{[b_1,b_2]}"{description}, from=4-3, to=3-1]
	\arrow["{[d_1,d_2]}"', from=4-3, to=3-3]
\end{tikzcd}\]
is called a \emph{boundary} pushout complement if $[c_1,c_2]$ is mono and there exist $d_1 : m \to L^{\bot}$ and $d_2 : n \to L^{\bot}$ making the above triangle commute and such that
\[
m \coprod j \xrightarrow{[d_1, c_2]} L^{\bot} \xleftarrow{[d_2, c_1]} n \coprod i
\] 
is an mda-cospan.
\end{definition}
Intuitively, the above definition ensures that after the second pushout is computed between the right-hand side of the rewrite rule and the pushout complement, outputs are glued to the inputs of the complement and vice versa, avoiding the problem we saw above.

\begin{proposition}[{\cite[Prop. 31]{bonchi_string_2022-2}}]
\label{prop:boundary_pushout_complement_unique}
  Let $i \xrightarrow{a_1} L \xleftarrow{a_2} j$ and $m \xrightarrow{b_1} G \xleftarrow{b_2} n$ be mda-cospans and mono $f : L \to G$.
  If a boundary pushout complement exists, then it is unique up to isomorphism.
\end{proposition}

The final obstacle that needs to be resolved is that not all sub-hypergraphs of a carrier of a cospan in $\catname{MHypI}(\Sigma)$ correspond to a sub-string diagram in $\mathbf{S}_{\Sigma}$.
\begin{example}[{\cite[Ex. 32]{bonchi_string_2022-2}}]
Consider $\Sigma_1 = \{e_1 : 1 \to 2, e_2 : 2 \to 1, e_3 : 1 \to 1, e_4 : 1 \to 1\}$ and the following rewrite rule in $\mathbf{S}_{\Sigma}$:
\[
\big\langle \adjustbox{scale=0.6}{\tikzfig{./figures/string/non_convex_match_left}} : 2 \to 2, \adjustbox{scale=0.7}{\tikzfig{./figures/string/non_convex_match_right}} : 2 \to 2 \big\rangle~.
\]
They are interpreted as the following cospans in $\catname{MHypI}(\Sigma)$:
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/non_convex_match_left_2}} \qquad \adjustbox{scale=0.7}{\tikzfig{./figures/string/non_convex_match_right_2}}~.
\]
Then, given the following diagram and its interpretation in $\catname{MHypI}(\Sigma)$
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/non_convex_match_source_diagram}} \; \xrightarrow{\llbracket - \rrbracket} \; \adjustbox{scale=0.7}{\tikzfig{./figures/string/non_convex_match_source}}~,
\]
we can get a DPOI square like below:
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/non_convex_dpoi}}~.
\]
The problem is that despite the pushout complement being a boundary complement and all the components being monogamous, the rewrite does not correspond to any rewrite in $\mathbf{S}_{\Sigma}$.
For this to be a rewrite in $\mathbf{S}_{\Sigma}$, $e_3$ must be moved either before $e_1$ or after $e_2$, but there is not enough structure to do this.
Of course, by~\cref{theorem:dpoi_rewriting_frob} the rewrite exists in $\mathbf{H}_{\Sigma}$:
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/non_convex_match_diagram_rewrite}}~.
\]
\end{example}

To fix the above issue, the notion of a match must be restricted to a \emph{convex match}.
\begin{definition}[{\cite[Def. 33]{bonchi_string_2022-2}}]
  A morphism $f : L \to G$ in $\catname{Hyp}(\Sigma)$ is a \emph{convex match} if it is mono and its image is a convex sub-hypergraph of $G$.
\end{definition}

\begin{definition}[{\cite[Def. 34]{bonchi_string_2022-2}}]
\label{def:convex_dpoi_rewriting}
Given $D \xleftarrow{} m \coprod n$ and $H \xleftarrow{} m \coprod n$ in $\catname{Hyp}(\Sigma)$, $D$ rewrites \emph{convexly} into $E$ with interface $m \coprod n$---written $D \Rrightarrow_{\mathcal{R}} E$---if there exist a rule $L \xleftarrow{} i \coprod j \xrightarrow{} R$ in $\mathcal{R}$ and an object $L^{\bot}$ and cospan arrows $i \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} n \coprod m$ such that the diagram below commutes and the marked squares are pushouts,
% https://q.uiver.app/#q=
\[\begin{tikzcd}
	L && {i \coprod j} && R \\
	\\
	G && {L^{\bot}} && H \\
	&& {m \coprod n}
	\arrow["f"', from=1-1, to=3-1]
	\arrow["{[a_1,a_2]}"', from=1-3, to=1-1]
	\arrow["{[b_1,b2]}", from=1-3, to=1-5]
	\arrow["{[c_1,c_2]}", from=1-3, to=3-3]
	\arrow[from=1-5, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=90}, draw=none, from=3-1, to=1-3]
	\arrow[from=3-3, to=3-1]
	\arrow[from=3-3, to=3-5]
	\arrow["\lrcorner"{anchor=center, pos=0.125, rotate=180}, draw=none, from=3-5, to=1-3]
	\arrow["{[q_1,q_2]}", from=4-3, to=3-1]
	\arrow["{[d_1,d_2]}"{description}, from=4-3, to=3-3]
	\arrow["{[p_1,p_2]}"', from=4-3, to=3-5]
\end{tikzcd}~,\]
and the following conditions hold:
\begin{enumerate}
  \item $f : L \to G$ is a convex match;
  \item $i \coprod j \xrightarrow{} L^{\bot}$ is a boundary complement in the leftmost pushout.
\end{enumerate}
\end{definition}
Note that $\Rrightarrow$ is contained in $\rightsquigarrow$ as some of the pushout complements in $\rightsquigarrow$ happen to be boundary complements and some of the matches happen to be convex.
Now we have all the ingredients to recall the soundness and completeness of rewriting $\Rrightarrow$ for $\Rightarrow$ in $\mathbf{S}_{\Sigma}$.
\begin{theorem}[{\cite[Thm. 35]{bonchi_string_2022-2}}]\label{theorem:convex_dpoi_rewriting}
Let $\mathcal{R}$ be any rewriting system on $\mathbf{S}_{\Sigma}$, then
\[
a \Rightarrow_{\mathcal{R}} b \qquad \iff \qquad \llangle \corner{a} \rrangle \Rrightarrow_{\llangle \corner{\mathcal{R}} \rrangle} \llangle \corner{b} \rrangle~.
\]
\end{theorem}
\begin{proof}
For the only-if direction,~\cref{theorem:dpoi_rewriting_frob} implies that 
\[
\llangle \corner{a} \rrangle \rightsquigarrow_{\llangle \corner{\mathcal{R}} \rrangle} \llangle \corner{b} \rrangle~,
\]
which gives us an analogous decomposition as in the proof of~\cref{theorem:dpoi_rewriting_frob}.
In particular, we have that
\[
\big\llangle \adjustbox{scale=0.7}{\tikzfig{./figures/string/convex_cup_decomposition}}\big\rrangle = i \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} m \coprod n~.
\]
We can check that $m \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} n \coprod i$ is an mda-cospan by \enquote{unbending} the wires:
\[
\adjustbox{scale=0.7}{\tikzfig{./figures/string/convex_cup_decomposition_2}}~.
\]
This is indeed monogamous as $a_1$ and $a_2$ are morphisms in $\mathbf{S}_{\Sigma}$.
Then, we have
\[
\llangle \corner{a} \rrangle
= (0 \xrightarrow{} G \xleftarrow{} m \coprod n)
= (0 \xrightarrow{} L \xleftarrow{} i \coprod j) \fatsemi (i \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} m \coprod n)
= \llangle \corner{l} \rrangle \fatsemi \llangle \tilde{a} \rrangle
\]
and let $f : L \to G$ be the pushout injection of the composition; we check that $f$ is mono and that its image is a convex sub-hypergraph of $G$.

For monicity, recall that the composition is computed by the pushout of
$L \xleftarrow{[a_1,a_2]} i \coprod j \xrightarrow{[c_1,c_2]} L^{\bot}$,
of which $f$ is the injection opposite to $[c_1,c_2]$.
Since $[c_1,c_2]$ is mono, as established when verifying the boundary
complement conditions, and pushouts along monos in $\catname{Hyp}(\Sigma)$
are mono, $f$ is mono.

For convexity, first observe that, by monogamy of $G$, a path that leaves
$f(L)$ can only do so through a vertex shared with $L^{\bot}$ whose unique
out-hyperedge lies in $L^{\bot}$, and can only re-enter $f(L)$ through a
shared vertex whose unique in-hyperedge lies in $L^{\bot}$.
The shared vertices are exactly $i \coprod j$; every $i$-vertex has its
out-hyperedge in $f(L)$, and every $j$-vertex has its in-hyperedge in
$f(L)$, so convexity reduces to the claim that $L^{\bot}$ contains no path
from a $j$-vertex to an $i$-vertex.
This is where we use that $\llangle \tilde{a} \rrangle$ arises from the
decomposition: the carrier $L^{\bot}$ is the carrier of $\llangle a_1
\rrangle$ glued to the carrier of $\llangle a_2 \rrangle$ along the
$k$-vertices only, with the $i$-vertices lying in the $a_1$-part and the
$j$-vertices in the $a_2$-part.
Assign rank $0$ to the hyperedges of the $a_1$-part and rank $1$ to those
of the $a_2$-part.
By monogamy of the two layers, every vertex of $L^{\bot}$ whose in- and
out-hyperedges both exist within $L^{\bot}$ has the in-hyperedge of rank
at most the out-hyperedge: internal vertices of a layer have both in the
same layer, and $k$-vertices have their in-hyperedge in the $a_1$-part and
out-hyperedge in the $a_2$-part.
Hence ranks are non-decreasing along paths.
A path starting at a $j$-vertex begins with a hyperedge of rank $1$, since
$j$-vertices are inputs of the $a_2$-part and carry no hyperedge of the
$a_1$-part; a path ending at an $i$-vertex ends with a hyperedge of rank
$0$, since the unique in-hyperedge of an $i$-vertex within $L^{\bot}$ lies
in the $a_1$-part.
No path can therefore lead from a $j$-vertex to an $i$-vertex, and $f(L)$
is convex.
That is, we have that $\llangle \corner{a} \rrangle \Rrightarrow_{\llangle \corner{\mathcal{R}} \rrangle} \llangle \corner{b} \rrangle$.

For the if direction, suppose $\llangle \corner{a} \rrangle \Rrightarrow_{\llangle \corner{\mathcal{R}} \rrangle} \llangle \corner{b} \rrangle$.
Then, by definition, there exists a convex match $f : L \to G$ and a boundary pushout complement $i \coprod j \xrightarrow{} L^{\bot} \xleftarrow{} m \coprod n$ such that the diagram commutes and the marked squares are pushouts, for some rule $\langle \llangle \corner{l} \rrangle, \llangle \corner{r} \rrangle \rangle$ in $\llangle \corner{\mathcal{R}} \rrangle$ with $\langle l, r \rangle : i \to j$ in $\mathcal{R}$.

Since $a$ is a morphism of $\mathbf{S}_{\Sigma}$, the unfolded cospan $m \xrightarrow{q_1} G \xleftarrow{q_2} n = \llbracket a \rrbracket$ is an mda-cospan by~\cref{theorem:monogamous_isomorphism}.
The match $f$ is mono with convex image, so~\cref{lemma:convex_decomposition} yields $k \in \mathbb{N}$ and a factorisation into mda-cospans
\[
(m \xrightarrow{} C_1 \xleftarrow{} k \coprod i)
\;\fatsemi\;
\bigl((k \xrightarrow{\id} k \xleftarrow{\id} k) \oplus (i \xrightarrow{} L \xleftarrow{} j)\bigr)
\;\fatsemi\;
(k \coprod j \xrightarrow{} C_2 \xleftarrow{} n)~,
\]
where the middle factor restricts to the unique cospan structure on $f(L) \cong L$ guaranteed by the lemma, compatible with the interface $i \coprod j \to L$ of the rule.
Applying~\cref{prop:monogamous_isomorphism} once more, this time using fullness, we obtain morphisms $a_1 : m \to k \coprod i$ and $a_2 : k \coprod j \to n$ in $\mathbf{S}_{\Sigma}$ with
\[
\llbracket a_1 \rrbracket = (m \xrightarrow{} C_1 \xleftarrow{} k \coprod i)
\qquad
\llbracket a_2 \rrbracket = (k \coprod j \xrightarrow{} C_2 \xleftarrow{} n)~.
\]
By functoriality, $\llbracket a \rrbracket = \llbracket a_1 \fatsemi (\id_k \otimes l) \fatsemi a_2 \rrbracket$, and since $\llbracket - \rrbracket$ is faithful, $a = a_1 \fatsemi (\id_k \otimes l) \fatsemi a_2$ already in $\mathbf{S}_{\Sigma}$.
This exhibits a redex, so the rule applies syntactically: setting $b' \defeq a_1 \fatsemi (\id_k \otimes r) \fatsemi a_2$, we have $a \Rightarrow_{\mathcal{R}} b'$.

It remains to show that $b' = b$.
The factorisation above assembles, exactly as in the only-if direction, into a second convex DPOI square for the same match $f$ and the same rule leg $i \coprod j \to L$, whose pushout complement is the folded context $i \coprod j \xrightarrow{} L^{\bot\prime} \xleftarrow{} m \coprod n$ built from $a_1$ and $a_2$, and whose result is $\llangle \corner{b'} \rrangle$.
By~\cref{prop:boundary_pushout_complement_unique}, boundary complements are unique, so $L^{\bot\prime}$ and $L^{\bot}$ are isomorphic as pushout complements, i.e.\ via an isomorphism commuting with the arrows from $i \coprod j$ and to $G$.
Pushing out $R \xleftarrow{} i \coprod j$ along the two isomorphic complements yields isomorphic cospans, hence
\[
\llangle \corner{b} \rrangle = (0 \xrightarrow{} E \xleftarrow{} m \coprod n) = \llangle \corner{b'} \rrangle~.
\]
Since $\llangle - \rrangle$ is an isomorphism of PROPs by~\cref{theorem:frobenius_isomorphism}, it follows that $\corner{b} = \corner{b'}$ in $\mathbf{H}_{\Sigma}$; unfolding, and using that $\mathbf{S}_{\Sigma} \to \mathbf{H}_{\Sigma}$ is faithful, $b = b'$ in $\mathbf{S}_{\Sigma}$.
Hence $a \Rightarrow_{\mathcal{R}} b$, as required.
\end{proof}

\begin{corollary}
\[
\mathbf{S}_{\Sigma, \mathcal{E}} \cong \catname{MHypI}(\Sigma) / \mathcal{R}~,
\]
where
\[
\mathcal{R} = \{ \langle \llangle \corner{l} \rrangle, \llangle \corner{r} \rrangle \rangle \mid l = r \in \mathcal{E} \} \cup \{ \langle \llangle r \rrangle, \llangle l \rrangle \rangle \mid l = r \in \mathcal{E}\}~,
\]
and $/ \mathcal{R}$ denotes the quotient of $\catname{MHypI}(\Sigma)$ by the rewriting rules $\rightsquigarrow_\mathcal{R}^{*}$ with $\rightsquigarrow^{*}$ being the reflexive transitive closure of $\rightsquigarrow$.
Namely, we say that $m_1 \xrightarrow{} \mathcal{G}_1 \xleftarrow{} n_1 \rightsquigarrow_{\mathcal{R}} m_2 \xrightarrow{} \mathcal{G}_2 \xleftarrow{} n_2$ if $0 \xrightarrow{} \mathcal{G}_1 \xleftarrow{} m_1 \coprod n_1 \rightsquigarrow 0 \xrightarrow{}_{\mathcal{R}} \mathcal{G}_2 \xleftarrow{} m_2 \coprod n_2$.
\end{corollary}

Hopefully the analogy between rewriting in a Lawvere theory (via $\mathbf{T}_{\Sigma}$) and rewriting in a PROP (via $\mathbf{S}_{\Sigma}$) is now clear and that hypergraphs, once equipped with an ability to express equivalences, are a natural representation for \emph{generalised} e-graphs, i.e. e-graphs for symmetric monoidal theories.
We lay out the foundations for this in the next chapter.

Notably, the machinery of DPOI rewriting as presented in this section has been further extended by either relaxing or restricting the class of cospans to accommodate morphisms in categories between $\mathbf{H}_{\Sigma}$ and $\mathbf{S}_{\Sigma}$.
This includes rewriting in $\mathbf{S}_{\Sigma}$ together with commutative (co)monoid structures---exactly half of the Frobenius algebra structure---presented by Milosavljevi\'{c}~\cite{zanassi_comonoid} who required monogamy on either the input interface or the output interface (and respectively restricted either in-degrees or out-degrees) together with an appropriate notion of a pushout complement.
Another example is the work of Kaye~\cite{ghica_rewriting_2023} who considered rewriting in $\mathbf{Tr}_{\Sigma}$---a syntactic symmetric monoidal category with trace comonoid structure.
The appropriate characterisation of morphisms as cospans considered partially monogamous cospans and traced boundary complements when performing DPOI rewriting.
